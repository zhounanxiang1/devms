# 内部需求开发管理系统 PRD

文档版本：V0.4 全局看板更新版  
创建日期：2026-07-15  
最近更新：2026-07-24  
适用对象：产品、业务、研发、测试、项目管理、运维、管理层

## 1. 背景与目标

内部需求开发通常涉及项目立项、持续需求迭代、开发测试、上线运维、持续优化、缺陷修复和版本发布。当前需要搭建一套统一系统，将项目、需求、任务、缺陷、版本、资料和人员组织纳入同一套管理流程。

本系统的核心目标：

1. 以项目为管理单元，承载一组持续迭代的需求，不以单次发版作为项目结束标志。
2. 统一归档项目资料，沉淀业务文档、技术文档、评审记录、上线材料和运维材料。
3. 规范需求从提出、评审、拆解、开发、测试、上线到关闭的全过程。
4. 支持 UI、前端、后端、数据等岗位人员基于需求领取任务、排期和协同。
5. 将缺陷与开发任务、版本关联，并通过任务追溯需求，保证缺陷修复可追踪、可排期、可复盘。
6. 通过版本管理明确每次上线包含哪些需求、修复哪些缺陷。
7. 管理组织架构、部门、人员、岗位和项目成员。
8. 按统一量化规则计算开发优先级，减少主观排序，提高排期透明度。

## 2. 产品范围

### 2.1 本期范围

本期建设以下模块：

1. 项目管理
2. 项目资料归档
3. 需求管理
4. 开发任务拆解与排期
5. 缺陷记录与分配
6. 版本管理
7. 组织人员管理
8. 开发优先级计算
9. 个人工作台、全局看板、后台管理、后台处理记录、查询筛选

### 2.2 暂不包含

1. 自动生成代码、自动化测试执行平台。
2. 与外部代码仓库、CI/CD、工单系统的深度集成。
3. 人力成本、预算、合同、采购管理。
4. 复杂项目集、项目组合和资源负载预测。

后续可根据使用情况扩展 Jira、GitLab、企业微信、飞书、禅道、测试平台等集成能力。

## 3. 组织、人员与岗位

本期不建设完整 RBAC、菜单权限配置或数据权限体系，但存在轻量权限控制：系统使用登录人员的岗位代码做功能入口和按钮控制，规则暂由代码写死，不在后台配置。组织架构、人员、岗位和项目成员关系仍用于人员归属、任务分工、责任人选择、统计筛选和协作展示；少量“负责人本人才能操作”的场景使用当前登录人与事项负责人是否一致进行判断。

| 对象 | 说明 |
| --- | --- |
| 组织架构 | 公司、部门、小组等层级结构，支持上下级关系 |
| 人员 | 系统内可被分配项目、需求、任务、缺陷的具体成员 |
| 登录账号 | 人员用于登录系统的账号，和人员档案绑定；账号本身不承载权限，权限依据绑定人员的岗位判断 |
| 岗位 | 人员的岗位或专业方向，如产品经理、业务、UI、前端、后端、数据、测试、运维等 |
| 项目成员 | 某人员在某项目中的参与关系，可记录项目内职责、负责范围、是否项目负责人；项目负责人是项目成员关系字段，不是岗位 |
| 岗位分工 | 用于任务拆解、筛选和轻量功能权限判断；权限规则暂由代码固定维护 |

## 4. 关键概念

### 4.1 项目

项目是围绕某一业务目标、系统能力或产品方向形成的一组迭代需求集合。项目不是一次开发发版完成后立即结束，而是可持续承载多个版本、多个需求和多轮优化。

### 4.2 需求

需求是项目下可被评审、拆解、开发、测试、上线的业务或技术诉求。一个项目可以包含多个需求，一个需求可以拆解为多个开发任务。

### 4.3 开发任务

开发任务是需求被拆解后的可执行工作项，支持按 UI、前端、后端、数据、测试、运维等类型划分和领取。

### 4.4 缺陷

缺陷是线下或线上过程中发现的问题。缺陷不直接关联需求，而是关联开发任务，并通过任务间接归属到需求；优先级计算通过任务关联的需求取得需求性质。

### 4.5 版本

版本记录某次发版的交付范围，包括上线需求、修复缺陷、发布时间、发布结论、风险和回滚方案。

## 5. 整体业务流程

1. 建立项目：产品经理录入项目名称、需求范围、项目负责人、计划开始时间、计划结束时间、期望上线时间等信息；当前阶段由系统默认设为已立项。
2. 归档资料：项目成员上传业务、技术、评审、测试、上线、运维等项目资料。
3. 提交需求：产品经理在项目下创建需求，系统自动带出所属项目且不可编辑，填写需求类型、需求性质、时效加分、期望上线时间、需求描述等，并实时查看只读优先级总分和计算逻辑。
4. 需求评审：组织评审并记录评审日期、结论、待补充事项和是否通过。
5. 任务拆解：评审通过后，产品经理、UI、前端、后端、数据、运维等岗位人员在需求列表行内创建开发任务，系统自动带出所属项目和关联需求且不可编辑。
6. 领取排期：研发人员领取任务，填写计划开始时间、计划完成时间、实际进度和阻塞信息。
7. 缺陷记录：产品经理或测试在任务行内创建缺陷，系统自动带出所属项目、关联需求、关联任务和任务负责人且不可编辑；缺陷优先级通过关联任务追溯需求性质。
8. 版本规划：产品经理或测试选择本次版本包含的需求和缺陷。
9. 上线发布：记录发版时间、上线内容、发布结果、风险、回滚方案和上线结论。
10. 持续优化：项目进入上线运维后，继续接收新需求、新缺陷和后续版本。
11. 项目结项：仅当该项目方向不再维护，且所有需求、缺陷、版本均处理完毕时，可将项目标记为已结项。

## 6. 功能需求

### 6.1 项目管理

#### 6.1.1 功能说明

支持创建、编辑、查询、归档和查看项目全景。项目创建后仍允许产品经理或项目负责人维护项目基本信息，但需求、任务、缺陷和版本应在项目内独立管理并保留操作记录。项目中心应集中展示项目基本信息、项目状态、计划周期、需求、任务、缺陷、资料、版本、成员和关键统计。

#### 6.1.2 关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 项目名称 | 项目展示名称 | 是 |
| 项目编号 | 系统自动生成，前端不提供手动录入入口，需唯一 | 是 |
| 项目负责人 | 对项目整体推进负责的人，仅允许选择产品经理岗位人员 | 是 |
| 需求范围 | 描述该项目承载的需求边界和业务范围，支持长文本换行 | 是 |
| 计划开始时间 | 项目整体计划启动时间，新建项目时默认当天 | 否 |
| 计划结束时间 | 项目整体计划完成时间 | 否 |
| 期望上线时间 | 当前阶段或首个版本的期望上线时间，保留为业务期望字段 | 否 |
| 实际开始时间 | 项目执行实际启动时间，由启动项目操作自动记录 | 否 |
| 实际结束时间 | 项目实际结项时间，由项目结项操作自动记录 | 否 |
| 当前阶段 | 页面展示为项目状态；新建项目时系统默认为已立项，不在创建或编辑项目表单中展示，不允许手动选择 | 是 |
| 项目背景 | 立项背景、业务问题、建设原因，支持长文本换行 | 否 |
| 项目目标 | 希望达成的业务或管理目标，支持长文本换行 | 否 |
| 涉及系统 | 关联业务系统、平台或服务，支持长文本换行 | 否 |
| 项目成员 | 项目内产品、研发、测试、运维等人员 | 否 |
| 创建人/创建时间 | 系统自动记录 | 是 |
| 更新时间 | 系统自动记录 | 是 |

#### 6.1.3 项目阶段

| 阶段 | 定义 |
| --- | --- |
| 已立项 | 项目已创建，目标和范围初步明确 |
| 进行中 | 项目已启动，正在持续承载需求、开发、测试、缺陷修复或版本准备 |
| 上线运维 | 已至少完成一次上线，进入持续优化和运维阶段 |
| 已结项 | 项目方向停止维护或阶段性完结，后续不再承载新需求 |

#### 6.1.4 业务规则

1. 一个项目可以有多个需求、多个版本、多个文档和多个成员。
2. 新建项目时，当前阶段固定为已立项，创建和编辑项目表单均不展示阶段字段，不允许手动选择阶段。
3. 新建项目时，计划开始时间默认当天，用户可按实际计划调整。
4. 新建项目时，项目负责人只能从产品经理岗位人员中选择。
5. 项目创建后，产品经理或该项目的项目负责人可以继续编辑项目名称、负责人、需求范围、计划时间、期望上线时间、背景、目标、涉及系统等项目信息；编辑操作必须留痕。
6. 项目阶段采用大颗粒度管理，不随单个需求、任务或缺陷频繁自动变化。
7. 产品经理或项目负责人可通过“启动项目”操作，将项目从已立项变为进行中，并自动记录实际开始时间；如实际开始时间已有值，则不重复覆盖。
8. 创建需求、评审需求、创建任务、创建缺陷均不自动改变项目阶段，具体进展由需求、任务、缺陷和版本状态体现。
9. 版本发布成功后，系统将项目阶段更新为上线运维。
10. 项目结项必须通过“项目结项”操作完成，允许产品经理或项目负责人操作；结项后项目阶段为已结项，并自动记录实际结束时间。
11. 项目进入已结项后，不允许新增需求、任务、缺陷、版本，也不允许发布版本；项目基本信息和项目资料仍可维护。
12. 已结项项目如需继续维护，允许产品经理或项目负责人重新打开；重新打开时可选择回到进行中或上线运维，并清空实际结束时间，保留原实际开始时间。
13. 项目阶段不能在普通编辑项目表单中直接修改，必须通过启动、发布、结项、重新打开等专门操作维护。
14. 项目删除应采用逻辑删除或归档，保留历史记录。
15. 项目计划开始时间、计划结束时间和期望上线时间均为业务计划字段，数据库层不做必填约束；是否必填由前端表单控制。项目实际开始时间和实际结束时间为系统维护字段，不在创建或编辑项目表单中手动填写。
16. 在项目中心内新建需求时，所属项目由当前选中的项目自动带出，只读展示，不允许在表单内切换。
17. 需求范围、项目背景、项目目标、涉及系统使用长文本录入和展示，保留换行，不提供富文本格式工具。
18. 当前版本项目结项后先冻结新增需求、新增任务、新增缺陷、新增版本和发布版本等入口；是否进一步冻结已存在任务、缺陷、需求、版本的编辑和状态流转，作为后续规则待确认项，暂不在本期实现。

#### 6.1.5 自动编号规则

系统内业务单据类对象由后端统一生成业务编号，前端表单不提供手动填写入口。编号用于列表展示、检索、问题沟通和历史追踪，不作为用户可编辑字段。

通用编号格式为：`对象前缀-YYYYMMDD-四位当日流水号`；需求编号因需要体现需求类型，使用专门格式 `REQ-YYYYMMDD-类型码-两位流水号`。

| 对象 | 前缀 | 示例 |
| --- | --- | --- |
| 项目 | PROJ | PROJ-20260721-0001 |
| 需求 | REQ | REQ-20260721-FN-01 |
| 开发任务 | TASK | TASK-20260721-0001 |
| 缺陷 | BUG | BUG-20260721-0001 |
| 版本 | VER | VER-20260721-0001 |
| 需求变更记录 | CHG | CHG-20260721-0001 |

通用编号生成规则：

1. `YYYYMMDD` 使用创建时服务器日期，不精确到时分秒。
2. 项目、开发任务、缺陷、版本、需求变更记录等对象的四位流水号按“对象前缀 + 日期”分别递增，每天从 `0001` 开始。
3. 流水号由数据库流水表维护，不使用随机数，不因服务重启而回退。
4. 数据库仍需保留唯一性校验；如发生并发冲突，系统应重新取号或提示重试。
5. 编号一旦生成不允许在普通编辑表单中修改，避免历史记录和沟通口径失真。
6. 组织、岗位属于基础主数据，通常沿用企业既有编码；当用户未填写时，系统可使用同一套 `ORG-YYYYMMDD-流水号`、`POS-YYYYMMDD-流水号` 规则兜底生成。
7. 项目资料当前以资料名称、类型、版本和附件归档为主，暂不单独生成资料编号；后续如需要资料编号，可按 `DOC-YYYYMMDD-流水号` 扩展。

需求编号规则：

1. 需求编号格式为 `REQ-YYYYMMDD-类型码-两位流水号`，例如 `REQ-20260721-FN-01`。
2. 两位流水号按“REQ + 需求类型码 + 日期”分别递增，同一天不同需求类型各自从 `01` 开始。
3. 需求类型码为两位大写字母：

| 需求类型 | 类型码 | 示例 |
| --- | --- | --- |
| 功能需求（FEATURE） | FN | REQ-20260721-FN-01 |
| 流程需求（PROCESS） | PR | REQ-20260721-PR-01 |
| 数据需求（DATA） | DA | REQ-20260721-DA-01 |
| 报表需求（REPORT） | RP | REQ-20260721-RP-01 |
| 体验优化（UX） | UX | REQ-20260721-UX-01 |
| 非功能需求（NON_FUNCTIONAL） | NF | REQ-20260721-NF-01 |

4. 后台若新增自定义需求类型，系统默认取字典编码去除非字母数字后的前两位作为类型码；不足两位时用 `X` 补齐，无法识别时使用 `OT`。

### 6.2 项目资料归档

#### 6.2.1 功能说明

项目资料归档是项目下的子板块，用于存放所有相关文档，包括业务资料和技术资料。资料以链接为主、附件上传为辅，支持分类、版本记录、关联需求/版本和搜索。

#### 6.2.2 资料类型

1. 业务需求文档
2. 调研记录
3. 会议纪要
4. 产品方案
5. 原型/设计稿
6. 技术方案
7. 接口文档
8. 数据文档
9. 测试用例/测试报告
10. 上线方案
11. 运维手册
12. 复盘材料
13. 其他

#### 6.2.3 关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 资料名称 | 文档名称 | 是 |
| 资料类型 | 业务、技术、测试、上线、运维等分类 | 是 |
| 所属项目 | 文档归属项目 | 是 |
| 资料描述 | 对资料内容、用途、范围做说明，支持长文本换行 | 否 |
| 上传附件 | 本地上传的文件附件，系统上传后保存附件访问地址 | 否 |
| 关联需求 | 可选关联一个或多个需求 | 否 |
| 关联版本 | 可选关联一个或多个版本 | 否 |
| 资料版本 | 如 V1.0、V1.1 | 否 |
| 创建人 | 系统自动记录 | 是 |
| 创建时间 | 系统自动记录 | 是 |
| 资料标签 | 用于补充标记资料主题、系统、业务线等 | 否 |

#### 6.2.4 业务规则

1. 文档不得游离于项目之外，必须归属某个项目。
2. 新增资料表单展示资料描述和上传附件；不再要求用户手工填写附件地址。
3. 上传附件由系统接收并保存，资料记录只保存附件访问地址；单个附件限制 50MB。
4. 附件查看采用预览弹窗；图片、PDF、文本/Markdown 等浏览器可识别文件支持在线预览，Office、压缩包等不适合浏览器内预览的文件提供下载入口，避免直接打开出现乱码。
5. 同一资料可保留多个历史版本，默认展示最新版。
6. 更新资料描述或上传新附件时，应保留旧版本记录和操作记录。
7. 资料删除应进入回收或归档状态，保留操作日志。

### 6.3 需求管理

#### 6.3.1 功能说明

需求管理在项目下进行，支持需求创建、评审、状态流转、优先级计算、任务拆解、版本纳入和上线关闭。

#### 6.3.2 关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 需求标题 | 需求名称 | 是 |
| 需求编号 | 系统自动生成，项目内唯一 | 是 |
| 所属项目 | 需求归属项目 | 是 |
| 需求类型 | 功能需求、流程需求、数据需求、报表需求、体验优化、非功能需求等 | 是 |
| 需求状态 | 待评审、评审通过、评审不通过、待补充、暂缓、开发中、已完成、已取消、需求变更、需求优化 | 是 |
| 上线状态 | 待上线、已上线 | 是 |
| 需求性质 | P0 基本需求、P1 期望需求、P2 兴奋需求、P3 无差异需求、P4 长期规划 | 是 |
| 时效加分 | 临时紧急场景下手工填写的加分，默认 0 | 否 |
| 时效加分原因 | 填写时效加分时说明业务原因 | 否 |
| 优先级总分 | 系统按需求性质、变更/优化场景和时效加分实时计算，只读展示 | 是 |
| 需求来源 | 用户手动输入 | 否 |
| 需求描述 | 需求背景、问题、目标和主要内容，支持长文本换行 | 是 |
| 验收标准 | 需求完成后的验收口径，支持长文本换行 | 是 |
| 评审日期 | 最近一次需求评审日期 | 否 |
| 评审结论 | 通过、不通过、补充材料、暂缓、取消等 | 否 |
| 评审记录 | 记录评审意见和待办事项 | 否 |
| 产品经理验收结论 | 产品经理手动验收完成时填写的验收结论 | 否，验收完成时必填 |
| 产品经理验收人/时间 | 系统自动记录执行验收完成的产品经理和时间 | 否 |
| UI 验收结论 | 存在 UI 任务时填写的 UI 验收结论 | 否，存在 UI 任务且验收完成时必填 |
| UI 验收人/时间 | 系统自动记录 UI 验收结论提交人和时间 | 否 |
| 期望上线时间 | 业务期望上线时间 | 否 |
| 实际上线时间 | 需求实际完成上线时间 | 否 |
| 优先级分数 | 系统按规则计算 | 是 |
| 提出人 | 系统记录需求创建人或提交人 | 是 |
| 负责人 | 本期新建需求表单暂不展示负责人字段，后续可按评审或分工补充 | 否 |
| 关联资料 | 从所属项目资料库中选择已上传的业务资料并关联到需求 | 否 |
| 需求补充记录 | 对需求提出阶段未覆盖内容的补充说明，包含标题、类型、原因、内容、影响范围/处理建议、创建人、创建时间 | 否 |

#### 6.3.3 需求状态流转

主状态推荐流转：

待评审 -> 评审通过 -> 开发中 -> 已完成

上线状态流转：

待上线 -> 已上线

异常流转：

1. 填写评审结论后，系统自动将需求流转为评审通过、评审不通过、待补充、暂缓或已取消。
2. 评审通过和开发中的需求允许创建开发任务；创建任务后，需求从评审通过流转为开发中。
3. 当需求下所有开发任务均为测试通过后，需求具备验收条件；产品经理手动点击“验收完成”并填写验收结论后，需求从开发中流转为已完成。
4. 版本发布关联需求后，需求上线状态从待上线流转为已上线；需求主状态不再承担待上线、已上线语义。
5. 产品经理在任何情况下可以触发需求变更或需求优化，原需求状态变为需求变更或需求优化，并视为终态。
6. 需求变更或需求优化触发后，原需求后续不允许再创建任务、创建缺陷、发布或完成。

#### 6.3.4 业务规则

1. 需求必须归属于项目。
2. 评审通过和开发中的需求允许正式拆解开发任务。
3. 需求性质 P0-P4 将影响新需求开发任务基础分，也会影响归属缺陷的加权系数。
4. 需求可以关联多个开发任务和一个或多个项目资料；缺陷不直接挂需求，而是挂在任务下。
5. 需求可以被纳入某个版本；一个需求如需拆分多次上线，应拆分为子需求或支持阶段交付记录。
6. 需求内容发生变更或优化时，不允许静默覆盖原始需求，应通过需求变更记录或新建优化需求留痕。
7. 需求状态原则上由创建、填写评审结论、拆解任务、验收完成、需求变更/优化等事件触发，不支持随意手动调整。
8. 在项目中心内新建需求时，所属项目只读展示，系统提交时使用当前项目 ID。
9. 新建需求表单暂不展示负责人字段，需求创建后如需明确跟进人，可在后续需求详情或任务拆解环节维护。
10. 创建开发任务入口放在需求表格行内；创建缺陷入口放在任务表格行内，确保缺陷具备明确任务上下文。
11. 新建需求时，上线状态默认为待上线。
12. 版本发布只允许关联需求状态为已完成且上线状态为待上线的需求；发布成功后仅更新上线状态为已上线。
13. 产品经理可在待评审或待补充需求上填写评审结果；评审结论提交后由系统自动更新需求状态。
14. 需求列表视图按状态严格筛选；评审通过和开发中分别作为独立状态展示，不混合统计。
15. 需求表格提供查看详情入口，详情展示需求基础信息、需求描述、验收标准、需求补充时间轴、评审信息、验收信息、关联资料，以及关联任务和关联缺陷标签页。
16. 产品经理只能在需求为开发中、需求下至少存在一个开发任务、且所有关联开发任务均为测试通过时点击验收完成；验收完成时产品经理验收结论必填，若存在 UI 任务则 UI 验收结论必填。
17. 产品经理可在需求已完成之前追加需求补充记录，用于补充需求提出阶段未考虑到的业务规则、边界条件、数据口径、验收口径或风险说明；补充记录不改变需求状态，不自动影响任务、缺陷或版本范围。

#### 6.3.5 需求补充管理

需求补充用于记录“原需求提出阶段未考虑到，但后续需要沉淀给设计、开发、测试或上线确认使用”的说明。它不同于需求变更/优化：补充记录只做信息追加和追溯，不代表范围重做，不触发关闭原需求下任务或缺陷。

##### 6.3.5.1 适用场景

1. 对业务规则、边界条件、数据口径、异常处理、验收口径进行补充说明。
2. 补充原需求中未明确但不改变主体范围的说明。
3. 记录评审、开发、测试过程中发现的说明缺口，方便后续查看和追溯。
4. 若补充内容会导致范围、方案、排期、优先级或版本范围发生实质变化，应走需求变更或需求优化，而不是仅追加补充记录。

##### 6.3.5.2 需求补充关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 关联需求 | 自动带出当前需求，不可编辑 | 是 |
| 补充标题 | 本次补充的简要标题，时间轴节点至少展示该字段 | 是 |
| 补充类型 | 内容补充、业务规则补充、边界条件补充、数据口径补充、验收口径补充、风险说明补充、其他补充 | 是 |
| 补充原因 | 为什么需要追加本次说明，支持长文本换行 | 否 |
| 补充内容 | 本次补充的具体说明，支持长文本换行 | 是 |
| 影响范围/处理建议 | 对设计、开发、测试、上线确认或资料归档的影响和建议，支持长文本换行 | 否 |
| 创建人/创建时间 | 系统自动记录 | 是 |

##### 6.3.5.3 展示规则

1. 需求详情页新增“需求补充”板块，使用时间轴展示补充记录。
2. 时间轴节点至少展示补充标题和创建时间，并展示创建人、补充类型。
3. 时间轴内容展示补充原因、补充内容、影响范围/处理建议。
4. 时间轴板块应设置内部最大高度和滚动条，记录较多时在板块内滚动，不无限撑开详情页。

#### 6.3.6 需求变更与优化管理

需求在评审后、开发中、上线前后均可能发生变更或优化。系统应支持产品经理在原需求上触发“需求变更”或“需求优化”，并通过表单创建一个新的需求，新需求关联原需求。

##### 6.3.6.1 适用场景

1. 需求描述、验收标准、业务规则、交互流程、字段口径发生调整。
2. 开发过程中发现原方案需要调整，影响 UI、前端、后端、数据、测试或上线计划。
3. 评审通过后新增范围、删除范围或调整优先级。
4. 上线后基于用户反馈、运营反馈、数据分析产生持续优化。
5. 原需求已上线或已完成，但仍需沉淀后续优化事项。

##### 6.3.6.2 变更记录关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 变更标题 | 本次变更或优化的简要标题 | 是 |
| 变更编号 | 系统自动生成 | 是 |
| 所属项目 | 自动继承原需求所属项目 | 是 |
| 原需求 | 被变更或被优化的需求 | 是 |
| 变更类型 | 澄清补充、范围新增、范围减少、方案调整、验收调整、上线后优化、其他 | 是 |
| 变更原因 | 说明为什么需要变更 | 是 |
| 变更前内容 | 原需求中的对应内容 | 是 |
| 变更后内容 | 本次拟调整后的内容 | 是 |
| 影响范围 | 需求、设计、开发、测试、版本、上线时间、资料、优先级等 | 是 |
| 影响说明 | 对开发范围、排期、风险、版本范围的具体影响 | 否 |
| 处理方式 | 当前需求内变更、新建优化需求、暂缓、驳回 | 是 |
| 评审日期 | 本次变更评审日期 | 否 |
| 评审结论 | 通过、不通过、待补充、暂缓 | 否 |
| 关联新需求 | 当处理方式为新建优化需求时关联 | 否 |
| 提出人/负责人 | 变更提出和跟进人员 | 是 |
| 创建时间/更新时间 | 系统自动记录 | 是 |

##### 6.3.6.3 处理策略

| 变更级别 | 判断标准 | 推荐处理方式 |
| --- | --- | --- |
| 小改动 | 文案补充、字段说明、验收口径澄清，不影响范围、排期和开发任务 | 在原需求内更新，并生成变更记录 |
| 范围变更 | 新增流程、调整规则、影响 UI/前端/后端/数据/测试内容 | 发起需求变更，原需求进入终态并关闭原任务/缺陷，重新创建关联原需求的新需求 |
| 较大优化 | 已明显超出原需求目标，或会显著影响排期、版本范围 | 新建优化需求，并关联原需求 |
| 上线后优化 | 需求已上线或已完成后产生的持续改进事项 | 新建优化需求，关联原需求、上线版本或反馈来源 |

##### 6.3.6.4 推荐流程

```text
提出变更/优化
-> 填写变更内容和原因
-> 评估影响范围：需求、设计、开发、测试、版本、上线时间、优先级
-> 组织变更评审
-> 确认处理方式：当前需求内变更 / 新建优化需求 / 暂缓 / 驳回
-> 同步更新需求、任务、缺陷、版本、资料和优先级
-> 记录变更历史和评审结论
```

##### 6.3.6.5 业务规则

1. 产品经理可在任何需求状态下触发需求变更或需求优化。
2. 触发后，原需求状态变为需求变更或需求优化，并视为终态，后续不允许再做任务、缺陷、发布、完成等操作。
3. 触发后，原需求下所有开发任务统一变为已关闭，任务下所有缺陷统一变为已关闭。
4. 触发时必须填写表单创建一个新的需求，新需求初始状态为待评审；上线状态允许用户选择，默认待上线。
5. 新需求需关联原需求，但需求内容由用户重新填写，不默认复制原需求字段。
6. 触发需求变更或需求优化时，应记录操作人、操作时间、原需求、新需求、处理类型和说明。
7. 已上线、已完成或已关闭的需求原则上不直接改写原需求范围；后续调整应通过需求变更或需求优化创建新需求并关联原需求。

### 6.4 开发任务拆解与排期

#### 6.4.1 功能说明

需求评审通过或开发中时，产品经理、UI、前端、后端、数据、运维等岗位人员可在需求列表行内创建开发任务。任务必须来源于某个需求，系统自动继承需求所属项目和关联需求。开发任务负责人本人可维护排期和进度。缺陷在任务下创建和跟踪。

#### 6.4.2 任务类型

1. UI 设计
2. 前端开发
3. 后端开发
4. 数据开发
5. 测试验证
6. 运维发布
7. 技术方案
8. 其他

#### 6.4.3 关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 任务标题 | 任务名称 | 是 |
| 任务编号 | 系统自动生成 | 是 |
| 所属项目 | 自动继承需求项目 | 是 |
| 关联需求 | 任务来源需求 | 是 |
| 任务类型 | 根据负责人主岗位自动带出，只读展示，不允许人工修改 | 是 |
| 任务状态 | 待处理、处理中、待测试、测试中、测试通过、已关闭 | 是 |
| 负责人 | 当前任务责任人 | 否 |
| 测试负责人 | 当前任务的测试责任人，只能选择测试岗位人员 | 是 |
| 计划开始时间 | 排期开始时间 | 否 |
| 计划完成时间 | 排期完成时间 | 否 |
| 实际开始时间 | 实际开始时间 | 否 |
| 实际完成时间 | 实际完成时间 | 否 |
| 开发说明 | 开发负责人点击开发完成时填写，说明实现范围、关键改动、联调情况和注意事项，支持长文本换行 | 是，开发完成时必填 |
| 阻塞原因 | 任务阻塞时填写 | 否 |
| 关联缺陷 | 当前任务下创建的缺陷 | 否 |
| 关联资料 | 开发完成时从所属项目资料库中选择已上传的技术资料，关联到开发任务详情 | 否 |
| 优先级分数 | 继承或关联需求/缺陷计算结果 | 是 |
| 创建人/创建时间 | 系统自动记录任务创建人和创建时间 | 是 |

#### 6.4.4 业务规则

1. 一个需求可以拆解多个任务。
2. 任务只能从需求表格行内创建，所属项目和关联需求由系统自动带出，只读展示，不允许在任务表单内切换。
3. 任务可由负责人处理，也可由项目负责人或技术负责人分配。
4. 新建任务时，负责人默认取当前登录人且允许人工修改；任务类型根据当前负责人主岗位自动带出，只读展示，不允许人工修改；若负责人未配置岗位，任务不能提交，系统不使用当前登录人岗位或默认岗位兜底。
5. 同一任务只能有一个主负责人，可支持多个协作人；测试负责人单独记录，只能从测试岗位人员中选择，新建任务时不默认带出且必填。
6. 任务列表应支持按优先级分数、计划时间、负责人、状态、任务类型筛选和排序。
7. 缺陷在任务下创建，任务详情和任务列表应能看到当前任务的缺陷数量和未验证缺陷。
8. 任务创建时状态为待处理。
9. 任务负责人点击开始处理后，任务从待处理流转为处理中，并自动填充实际开始时间。
10. 任务负责人点击处理完成时，弹出开发完成表单，必须填写开发说明；可从当前项目资料库中选择已上传的技术资料，并自动关联到该开发任务。
11. 开发完成表单提交后，任务从处理中流转为待测试，并自动填充实际完成时间。
12. 产品经理或测试点击开始测试后，任务从待测试流转为测试中；若任务测试负责人为空，则任何人都不能开始测试。
13. 产品经理或测试点击测试通过后，任务从测试中流转为测试通过；若任务测试负责人为空，则任何人都不能确认测试通过。
14. 点击测试通过前，任务下所有缺陷必须为已验证或已关闭，或该任务下无缺陷记录，否则拦截测试通过。
15. 当需求下所有开发任务均为测试通过时，需求保持开发中并进入可验收状态；产品经理完成验收后，需求才从开发中流转为已完成。
16. 当前任务负责人本人可手动关闭任务，且仅允许在待处理或处理中状态操作；需求变更或需求优化触发时，原需求下所有任务自动变为已关闭。
17. 新建开发任务时，计划开始时间默认当天，仍允许人工调整；计划开始时间和计划完成时间均为前端表单字段，其中计划开始时间非必填。若所属项目维护了计划开始时间，则开发任务排期不能早于该日期；若所属项目维护了计划结束时间，则开发任务排期不能晚于该日期；项目计划时间未填写时不作为排期限制。
18. 新建开发任务表单中，关联需求下方应以普通只读字段展示“需求优先级分数”和“优先级排序”。“优先级排序”按当前需求分数放入所选负责人全部开发任务、缺陷修复任务后的分数排名计算。
19. 新建开发任务表单不直接嵌入负责人待处理事项列表，改为提供“查看排期情况”按钮。点击后以可复用甘特图弹窗展示所选负责人名下开发任务和缺陷修复排期。
20. 甘特图默认展示开发任务的待处理、处理中状态，以及缺陷修复的待修复、修复中状态；用户可通过多选下拉按任务状态和缺陷状态自行选择展示范围，不提供“恢复默认状态”按钮。
21. 甘特图事项信息应以左侧固定信息表 + 右侧横向时间轴展示，横向滚动日期时左侧事项信息不能被卷走；左侧默认只展示核心列：事项名称、类型、状态、起止时间、优先级分数。所属项目、关联需求、关联任务作为事项摘要和悬浮详情展示，不强制全部展开为独立列，避免信息区过长挤压时间轴。开发任务展示其关联需求，缺陷修复展示其关联需求和关联任务，不使用“关联对象”等含义不清的字段名。
22. 甘特图左侧信息表宽度支持用户拖拽调整，系统应设置合理的最小/最大宽度，保证事项信息和右侧时间轴都可读。
23. 甘特图支持两种排期调整方式：一是拉动排期条最左侧和最右侧边界，分别快速调整计划开始时间和计划结束/完成时间；二是在左侧信息表中直接修改起止日期，日期修改后右侧甘特图应立即联动变化。拖拽手柄应采用轻量边界样式，不影响时间条阅读。
24. 新建开发任务时，当前表单中的任务标题、所属项目、关联需求、优先级分数、计划开始时间和计划完成时间应作为高亮临时事项纳入甘特图，并按优先级分数参与统一排序，不固定置顶；用户调整临时事项排期并保存后，只同步回当前表单的计划开始/完成时间，不提前创建正式任务，正式任务仍需在表单提交成功后创建。
25. 甘特图排期调整权限限定为负责人本人，登录人只能调整自己名下的开发任务和缺陷修复排期；查看他人排期时仅允许查看，不允许调整和保存。
26. 开发任务仅在待处理或处理中状态允许调整排期；其他状态可查看排期但不可修改。排期调整必须落在已维护的项目计划时间边界内，并记录调整前后时间、调整人、调整时间和调整原因。
27. 开发任务提供只读开发详情页，展示归属需求、所属项目、负责人、状态、优先级分数、计划/实际时间、开发说明、关联资料和任务下缺陷概况。

### 6.5 缺陷记录

#### 6.5.1 功能说明

支持记录线下和线上过程中发现的缺陷。项目内缺陷从任务表格行内创建，系统自动带出所属项目、关联需求、关联任务和任务负责人且不可编辑；缺陷支持查看详情、开始修复、修复完成、验证、关闭和开启。

#### 6.5.2 关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 缺陷标题 | 缺陷简要描述 | 是 |
| 缺陷编号 | 系统自动生成 | 是 |
| 所属项目 | 缺陷归属项目 | 是 |
| 关联任务 | 缺陷归属的开发任务 | 是 |
| 关联需求 | 通过任务自动继承，仅用于展示和优先级计算，不直接编辑 | 是 |
| 关联版本 | 发现缺陷或计划修复的版本 | 否 |
| 缺陷等级 | 阻塞、严重、一般、次要 | 是 |
| 缺陷状态 | 待修复、修复中、已修复、已验证、已关闭 | 是 |
| 负责人 | 默认继承开发任务负责人，创建缺陷时不手动选择 | 是 |
| 测试负责人 | 当前缺陷的验证责任人，默认当前登录人，可修改为产品经理或测试人员 | 是 |
| 发现时间 | 缺陷被发现的业务时间 | 否 |
| 发现入口 | 页面、菜单、接口、批处理、报表等问题入口 | 否 |
| 影响范围 | 影响用户、业务流程、数据范围、功能模块或版本范围 | 否 |
| 前置条件 | 复现前需要满足的数据、账号、开关、配置或权限条件 | 否 |
| 计划开始时间 | 缺陷修复排期开始时间，由开发负责人后续维护 | 否 |
| 计划结束时间 | 缺陷修复排期结束时间，由开发负责人后续维护 | 否 |
| 缺陷描述 | 缺陷现象和影响，支持长文本换行 | 是 |
| 复现步骤 | 可复现缺陷的操作路径 | 否 |
| 实际结果 | 当前错误表现 | 否 |
| 期望结果 | 正确表现 | 否 |
| 设备/浏览器/客户端版本 | 终端型号、浏览器、系统版本、App 版本等定位信息 | 否 |
| 测试账号/样例数据 | 可用于复现的数据样例、账号、订单号、请求参数等 | 否 |
| 发现环境 | 系统自动判断：关联需求已上线时默认为线上，未上线时默认为线下；列表和详情展示，创建和编辑表单不展示 | 是 |
| 附件 | 截图、日志、录屏等 | 否 |
| 实际开始修复时间 | 点击开始修复时系统自动记录；历史修复中数据在点击已修复时兜底补齐 | 否 |
| 实际完成修复时间 | 点击已修复时系统自动记录 | 否 |
| 优先级分数 | 系统按缺陷等级、自动判断的环境基础分、归属需求系数和时效加分实时计算，只读展示 | 是 |
| 创建人/创建时间 | 系统自动记录缺陷创建人和创建时间 | 是 |

#### 6.5.3 缺陷等级

| 等级 | 定义 |
| --- | --- |
| 阻塞 | 核心流程无法继续、系统不可用、资损或高危安全问题 |
| 严重 | 核心流程局部异常、高频操作失败、关键数据错误 |
| 一般 | 功能可继续使用，但存在普通流程异常或体验问题 |
| 次要 | 轻微样式、文案、提示、兼容性或低影响问题 |

#### 6.5.4 业务规则

1. 缺陷必须归属于任务，并通过任务归属于项目和需求。
2. 缺陷创建时状态为待修复。
3. 任务负责人点击开始修复后，缺陷从待修复流转为修复中，并自动填充实际开始修复时间。
4. 任务负责人点击已修复后，缺陷从修复中流转为已修复，并自动填充实际完成修复时间；如历史数据缺少实际开始修复时间，系统同步兜底补齐。
5. 产品经理或测试点击验证通过后，缺陷从已修复流转为已验证。
6. 产品经理或测试点击验证未通过后，缺陷从已修复流转为修复中。
7. 产品经理或测试可看到关闭按钮；仅待修复或修复中状态允许点击关闭，其他状态置为禁用态并说明原因。
8. 已关闭缺陷仅提供开启功能，开启后状态变为待修复。
9. 线上环境缺陷优先级通常高于线下环境缺陷。
10. 缺陷优先级通过关联任务追溯到需求性质进行加权计算。
11. 任务点击测试通过前，任务下缺陷必须全部处于已验证或已关闭状态，或无缺陷记录。
12. 创建缺陷时不填写计划开始时间和计划结束时间，缺陷排期由开发任务负责人后续维护。
13. 缺陷修复仅在待修复或修复中状态允许维护计划开始时间和计划结束时间；若所属项目维护了计划开始时间，则缺陷修复排期不能早于该日期；若所属项目维护了计划结束时间，则缺陷修复排期不能晚于该日期；项目计划时间未填写时不作为排期限制。排期调整需在操作日志中保留调整记录。
14. 缺陷创建时负责人固定继承关联开发任务负责人；测试负责人默认取当前登录人，允许修改为产品经理或测试岗位人员。
15. 创建和编辑缺陷时不展示发现环境字段，由系统根据关联需求上线状态自动取值：已上线需求默认为线上，未上线需求默认为线下；列表和详情可以展示该系统判断结果。
16. 只有测试中或测试通过的开发任务允许创建缺陷。
17. 当测试通过的任务被创建线下缺陷时，该任务自动从测试通过退回测试中；如果对应需求已完成，则需求同步退回开发中，待任务再次测试通过后由产品经理重新验收完成。
18. 当测试通过的任务被创建线上缺陷时，关联需求上线状态必须为已上线；创建成功后原任务保持测试通过，原需求保持已完成和已上线，线上缺陷通过缺陷修复和版本发布闭环。
19. 若后续需要区分已上线需求下的线下回归缺陷，可新增专门的环境修正能力；当前版本不在缺陷表单中提供发现环境编辑入口。
20. 缺陷提供只读详情页，展示归属项目、关联需求、关联任务、负责人、创建人、创建时间、发现时间、环境、等级、优先级分数、现象影响、复现定位、环境附件和计划修复排期。

### 6.6 版本管理

#### 6.6.1 功能说明

版本管理用于记录每次发版计划、上线范围和上线结果。每个版本需要明确上线了哪些需求、修复了哪些缺陷。

#### 6.6.2 关键字段

| 字段 | 说明 | 必填 |
| --- | --- | --- |
| 版本名称 | 如 2026.07.15 正式版、V1.2.0 | 是 |
| 版本编号 | 项目内唯一 | 是 |
| 所属项目 | 版本归属项目 | 是 |
| 版本类型 | 常规版本、紧急修复、灰度版本、回滚版本 | 是 |
| 版本状态 | 规划中、开发中、测试中、待发布、已发布、已回滚、已取消 | 是 |
| 计划发版时间 | 预计发版时间 | 是 |
| 实际发版时间 | 版本发布成功时由系统自动记录 | 否 |
| 上线需求 | 本版本上线的需求列表 | 是 |
| 修复缺陷 | 本版本修复的缺陷列表 | 否 |
| 发布说明 | 面向内部的发版说明 | 否 |
| 发布负责人 | 本次发布负责人 | 是 |
| 风险说明 | 发布风险、影响范围 | 否 |
| 回滚方案 | 失败后的回滚处理方式 | 否 |
| 上线结论 | 成功、失败、部分成功、回滚 | 否 |
| 创建人/创建时间 | 系统自动记录版本创建人和创建时间 | 是 |

#### 6.6.3 业务规则

1. 版本必须归属于项目。
2. 一个版本可以包含多个需求和多个缺陷。
3. 版本只能选择当前项目下的需求和缺陷，不允许跨项目混选上线范围。
4. 新建或编辑版本时，上线需求和修复缺陷采用弹窗表格多选，不在表单内直接铺开大量选项；弹窗支持关键词查询、枚举筛选、排序、分页和勾选。
5. 上线需求可选范围仅限当前项目下需求状态为已完成且上线状态为待上线的需求；修复缺陷可选范围仅限当前项目下状态为已验证或已关闭的缺陷。
6. 后端创建、编辑和发布版本时均需校验版本范围，禁止绕过前端提交非当前项目或不满足状态条件的数据。
7. 同一需求如跨版本交付，应拆分子需求或记录阶段交付说明。
8. 版本仅维护计划发版时间和实际发版时间，不设置版本实际开始时间、实际结束时间；实际发版时间在发布成功时由系统自动记录。
8. 版本发布前，应检查需求主状态、需求上线状态和缺陷状态。
9. 版本发布后，系统应自动记录上线范围快照，避免后续需求变更影响历史发版记录。
10. 版本已发布后，需求和缺陷的版本归属变更需记录操作日志。
11. 版本内所有需求必须为需求状态已完成且上线状态待上线，否则拦截发布。
12. 版本内所有缺陷必须为已验证或已关闭，否则拦截发布。
13. 版本发布成功后，关联需求上线状态变为已上线。
14. 创建、编辑和发布版本操作仅允许岗位为产品经理或测试的人员执行。
15. 版本已发布、已回滚或已取消后，不允许重复发布。

### 6.7 组织人员与岗位管理

#### 6.7.1 功能说明

维护内部组织架构、部门、小组、人员、岗位、登录账号和项目成员关系，支持人员登录系统、参与项目、领取任务、处理缺陷和查看统计。本期不建设可配置权限体系，但岗位会作为轻量功能权限判断依据；登录账号只提供身份识别和登录能力，权限取绑定人员的岗位。

#### 6.7.2 关键字段

| 对象 | 字段 |
| --- | --- |
| 组织架构 | 组织名称、组织编码、上级组织、组织负责人、组织状态、排序 |
| 部门/小组 | 部门名称、所属组织、上级部门、部门负责人、状态 |
| 岗位 | 岗位名称、岗位编码、岗位类别、岗位说明、状态 |
| 人员 | 姓名、员工编号、手机号/邮箱、所属组织、所属部门、岗位、直属负责人、在职状态 |
| 登录账号 | 登录账号、绑定人员、账号状态、是否允许登录、初始密码状态、密码更新时间、最后登录时间、账号备注 |
| 人员岗位关系 | 人员、岗位、是否主岗位、生效时间、失效时间 |
| 项目成员 | 项目、人员、项目内职责、负责范围、加入时间、退出时间、是否项目负责人 |

#### 6.7.3 业务规则

1. 组织架构支持多级层级，可维护公司、部门、小组等节点。
2. 人员必须归属到一个组织或部门。
3. 人员可关联一个主岗位，也可关联多个兼任岗位。
4. 岗位用于任务拆解、负责人筛选、统计分组和项目成员职责展示。
5. 项目成员用于记录人员参与项目的关系，可维护项目内职责和负责范围。
6. 每个人员可绑定一个登录账号；如需要支持多人共用或一个人员多个账号，需另行确认。
7. 登录账号支持启用、停用、锁定、解锁、重置密码和解除绑定。
8. 停用或离职人员不再作为新任务、新缺陷、新需求的负责人候选项，其登录账号默认同步停用，历史数据保留。
9. 登录账号仅用于身份识别和系统登录，不直接承载权限；功能权限依据绑定人员的岗位判断。
10. 本期不建设完整权限配置体系，岗位权限规则由代码固定维护。
11. 后台管理入口仅允许岗位为产品经理的人员进入。
12. 缺陷验证通过、缺陷开启、任务开始测试、任务测试通过等操作仅允许岗位为测试或产品经理的人员执行。
13. 版本创建、编辑和发布操作仅允许岗位为产品经理或测试的人员执行。
14. 开发任务开始处理、处理完成和排期调整仅允许任务负责人本人执行；缺陷开始修复、标记已修复和排期调整仅允许缺陷负责人本人执行。

### 6.8 功能按钮显隐与可用规则

按钮统一受两类规则控制：

1. 权限控制：基于当前登录人员岗位或是否为事项负责人判断。不满足权限时，前端不展示按钮；后端接口必须同步拦截。
2. 功能控制：基于项目、需求、任务、缺陷、版本的业务状态判断。满足权限但不满足功能条件时，按钮仍展示，但前端置为禁用态，并通过提示说明原因。

本期权限不是可配置角色权限，采用代码写死的轻量岗位规则。登录账号只用于识别当前人员，权限取人员岗位；数据范围暂不做组织、项目成员或部门级隔离。

责任人字段是权限判断的重要依据。凡是权限规则写明“当前负责人本人”的操作，如果数据负责人为空，则视为没有任何人拥有该操作权限，前端不展示对应按钮，后端接口必须拒绝执行。新增或编辑表单中，凡是用于权限判断、任务分配或工作台归属的责任人字段必须必填，不能通过空值形成公共可操作数据。

岗位也是权限判断的重要依据。当前登录人员没有对应岗位，或用于业务推导的人员未配置岗位时，不允许通过默认岗位、当前登录人岗位或系统兜底值绕过校验。开发任务类型依赖任务负责人岗位自动带出；若所选负责人未配置岗位，任务不能提交。任务测试负责人和缺陷测试负责人是测试责任归属字段；历史数据中该字段为空时，测试、验证、关闭、开启等测试责任相关动作视为无人可操作。

| 对象 | 按钮/操作 | 权限控制，不满足隐藏 | 功能控制，不满足禁用 |
| --- | --- | --- | --- |
| 项目 | 新建项目 | 产品经理 | 无 |
| 项目 | 编辑项目 | 产品经理或该项目负责人 | 无 |
| 项目 | 启动项目 | 产品经理或该项目负责人 | 项目状态必须为已立项 |
| 项目 | 项目结项 | 产品经理或该项目负责人 | 项目状态不能为已结项 |
| 项目 | 重新打开 | 产品经理或该项目负责人 | 项目状态必须为已结项 |
| 项目 | 新建需求 | 产品经理 | 项目不能为已结项 |
| 项目 | 新建资料 | 登录用户 | 当前项目存在；已结项项目仍可维护资料 |
| 项目 | 新建版本 | 产品经理或测试 | 项目不能为已结项 |
| 需求 | 填写评审结果 | 产品经理 | 需求状态必须为待评审或待补充 |
| 需求 | 查看详情 | 登录用户 | 无 |
| 需求 | 验收完成 | 产品经理 | 需求状态必须为开发中；至少存在一个开发任务；所有关联开发任务必须为测试通过；产品经理验收结论必填；存在 UI 任务时 UI 验收结论必填 |
| 需求 | 需求补充 | 产品经理 | 需求状态必须为待评审、评审通过、评审不通过、待补充、暂缓或开发中；已完成、已取消、需求变更、需求优化不可补充 |
| 需求 | 创建开发任务 | 产品经理、UI、前端、后端、数据、运维 | 项目不能为已结项；需求状态必须为评审通过或开发中；需求不能处于需求变更或需求优化终态 |
| 需求 | 需求变更 | 产品经理 | 项目不能为已结项；原需求不能处于需求变更或需求优化终态 |
| 需求 | 需求优化 | 产品经理 | 项目不能为已结项；原需求不能处于需求变更或需求优化终态 |
| 开发任务 | 排期 | 当前任务负责人本人；任务负责人为空时无人可操作 | 任务状态必须为待处理或处理中 |
| 开发任务 | 开始处理 | 当前任务负责人本人；任务负责人为空时无人可操作 | 任务状态必须为待处理 |
| 开发任务 | 处理完成 | 当前任务负责人本人；任务负责人为空时无人可操作 | 任务状态必须为处理中，提交时填写开发说明，可关联当前项目资料库中的技术资料 |
| 开发任务 | 开始测试 | 产品经理或测试；任务测试负责人为空时无人可操作 | 任务状态必须为待测试 |
| 开发任务 | 测试通过 | 产品经理或测试；任务测试负责人为空时无人可操作 | 任务状态必须为测试中；任务下缺陷必须全部为已验证或已关闭，或无缺陷 |
| 开发任务 | 手动关闭 | 当前任务负责人本人；任务负责人为空时无人可操作 | 任务状态必须为待处理或处理中 |
| 开发任务 | 创建缺陷 | 产品经理或测试 | 项目不能为已结项；任务状态必须为测试中或测试通过；关联任务负责人不能为空 |
| 缺陷 | 排期 | 当前缺陷负责人本人；缺陷负责人为空时无人可操作 | 缺陷状态必须为待修复或修复中 |
| 缺陷 | 开始修复 | 当前缺陷负责人本人；缺陷负责人为空时无人可操作 | 缺陷状态必须为待修复 |
| 缺陷 | 已修复 | 当前缺陷负责人本人；缺陷负责人为空时无人可操作 | 缺陷状态必须为修复中 |
| 缺陷 | 验证通过 | 产品经理或测试；缺陷测试负责人为空时无人可操作 | 缺陷状态必须为已修复 |
| 缺陷 | 验证未通过 | 产品经理或测试；缺陷测试负责人为空时无人可操作 | 缺陷状态必须为已修复 |
| 缺陷 | 关闭 | 产品经理或测试；缺陷测试负责人为空时无人可操作 | 缺陷状态必须为待修复或修复中 |
| 缺陷 | 开启 | 产品经理或测试；缺陷测试负责人为空时无人可操作 | 缺陷状态必须为已关闭 |
| 版本 | 发布版本 | 产品经理或测试 | 项目不能为已结项；版本不能为已发布、已回滚或已取消；版本内需求必须为已完成且上线状态为待上线；版本内缺陷必须为已验证或已关闭 |
| 全局看板 | 进入全局看板、查看项目/人员/风险统计 | 产品经理 | 看板数据受全局筛选和后台看板规则配置影响 |
| 后台管理 | 进入后台、新增和编辑基础配置 | 产品经理 | 字典、人员、组织、岗位、账号、优先级配置按各自表单规则校验 |

按钮规则需要前后端保持一致：前端负责展示和禁用态，后端负责最终权限和业务状态校验。

### 6.9 表单暂存、提交与字段状态

#### 6.9.1 功能说明

系统内新增、编辑类表单需要区分“暂存草稿”和“提交”。未填写完毕时，用户可先暂存草稿；点击提交后才正式写入业务数据并触发状态流转、日志和校验。

#### 6.9.2 业务规则

1. 表单提供“暂存草稿”和“提交”两个动作。
2. 暂存草稿不要求用户填写完整必填字段，不触发正式业务提交，不创建正式项目、需求、任务、缺陷、版本或后台配置数据。
3. 提交时才执行前端必填校验、字段格式校验和业务规则校验。
4. 草稿可在用户关闭弹窗或刷新页面后继续恢复；正式提交成功后，对应草稿自动清除。
5. 表单应提供清除草稿入口，允许用户主动放弃暂存内容。
6. 密码类字段不得明文写入草稿。
7. 必填字段以明显样式标识；必填规则只作为前端表单控制，不要求数据库层创建非空约束。
8. 不可编辑字段应使用只读展示样式，同时以隐藏字段携带系统提交所需 ID，避免用户误以为可以修改。
9. 从项目、需求等上下文进入的表单，草稿应按上下文隔离，避免 A 项目或 A 需求的草稿串到 B 项目或 B 需求。
10. 新增、编辑、评审、排期调整、结项确认等表单统一采用居中弹窗展示，避免侧拉窗在复杂表单下造成阅读和操作割裂。
11. 表单内选择系统业务对象的字段必须支持手动输入关键词查询，包括但不限于项目、需求、任务、人员、组织、岗位、版本、上线需求、修复缺陷等；固定字典类字段如状态、类型、等级不强制使用搜索。

## 7. 开发优先级计算

### 7.1 核心原则

1. 所有任务统一量化优先级分数，开发按优先级分数从高到低排期，减少人工主观判断。
2. 所有缺陷关联所属任务，并通过任务追溯需求，需求等级决定加权系数，实现高优需求缺陷天然优先。
3. 需求分值由需求性质、变更/优化场景和手动时效加分共同构成，避免已上线需求变更被普通优化低估。
4. 同优先级分数时启用兜底规则：缺陷修复任务优先于新需求开发任务；缺陷同分或需求同分时再按细则排序。

### 7.2 新需求基础分值

| 需求等级 | 等级定义 | 新需求基础分值 |
| --- | --- | --- |
| P0 基本需求 | 线上止损、资损、合规整改、重大限时核心活动 | 40 |
| P1 期望需求 | 迭代主线、核心业务流程、团队 OKR 重点功能 | 30 |
| P2 兴奋需求 | 次要功能迭代、普通用户体验优化 | 20 |
| P3 无差异需求 | 后台小优化、边缘冷门功能、非刚需体验 | 10 |
| P4 长期规划 | 调研规划、长期储备、可无限延后 | 0 |

### 7.2.1 需求变更/优化场景加分

需求变更或需求优化创建出来的新需求，需要在基础分上叠加场景加分。普通新需求不叠加该项。

| 场景 | 加分 |
| --- | --- |
| 普通新需求 | 0 |
| 待上线需求优化 | 3 |
| 待上线需求变更 | 6 |
| 已上线需求优化 | 6 |
| 已上线需求变更 | 15 |

说明：

1. 场景由新需求的变更/优化类型和上线状态共同决定。
2. 已上线需求变更通常意味着线上能力已被使用，本次调整更容易影响存量业务，因此加分最高。
3. 已上线需求优化默认只表达普通线上持续改进影响，不天然视为高紧急；若属于投诉、转化下降、性能瓶颈等紧急优化，应通过时效加分补足。
3. 场景加分只用于补足紧急性判断，不替代 P0-P4 需求性质判断。
4. 场景加分为系统固定规则，不放入后台字典维护；如需临时特殊加急，仍通过手动时效加分处理。

### 7.3 缺陷基础分值

缺陷基础分值为缺陷等级与发现环境对应的基础分值，后续将乘以归属需求加权系数。

| 缺陷等级 | 等级定义 | 线上环境基础分 | 线下环境基础分 |
| --- | --- | --- | --- |
| 阻塞 | 核心流程无法继续、系统不可用、资损或高危安全问题 | 60 | 40 |
| 严重 | 核心流程局部异常、高频操作失败、关键数据错误 | 45 | 30 |
| 一般 | 功能可继续使用，但存在普通流程异常或体验问题 | 25 | 15 |
| 次要 | 轻微样式、文案、提示、兼容性或低影响问题 | 10 | 5 |

### 7.4 归属需求加权系数

| 归属需求等级 | 加权系数 |
| --- | --- |
| P0 紧急 | 1.20 |
| P1 核心 | 1.15 |
| P2 常规 | 1.10 |
| P3 低优 | 1.03 |
| P4 远期/无归属 | 1.00 |

### 7.5 通用时效加分项

时效加分在基础分或加权分之上直接累加，用于适配临时紧急场景。本期不维护时效加分规则，时效加分作为需求或缺陷上的手工填写分值。

时效加分字段要求：

1. 默认为 0。
2. 支持手工录入正数或 0。
3. 修改时效加分时必须填写调整原因。
4. 系统记录调整前分值、调整后分值、调整人和调整时间。
5. 时效加分变更后，自动重新计算优先级分数。

### 7.6 统一优先级分数计算公式

需求开发任务：

```text
优先级分数 = 需求基础分 + 需求变更/优化场景加分 + 时效叠加加分
```

缺陷修复任务：

```text
优先级分数 = (缺陷环境基础分 x 归属需求加权系数) + 时效叠加加分
```

### 7.7 同分兜底排序规则

当两个或多个任务优先级分数一致时，按以下顺序排序：

1. 缺陷修复任务优先于全新需求开发任务。
2. 同为缺陷时，线上环境优先于线下环境。
3. 同环境、同归属需求时，缺陷严重等级越高越优先。
4. 所有条件一致时，任务创建时间越早，优先级越高。

### 7.8 计算触发时机

系统应在以下场景自动重新计算优先级分数：

1. 需求性质变更。
2. 缺陷等级变更。
3. 缺陷发现环境变更。
4. 缺陷关联任务变更。
5. 手工时效加分变更。
6. 任务创建、需求拆解、缺陷分配。
7. 触发需求变更或需求优化并创建新需求，且影响需求性质、上线状态、期望上线时间、时效加分、任务范围或版本范围。
8. 项目负责人或系统维护人员手动触发重算。

### 7.9 排期展示要求

1. 任务列表默认按优先级分数倒序展示。
2. 展示优先级分数、基础分、变更/优化场景加分、加权系数、时效加分和计算明细。
3. 支持按项目、负责人、任务类型、需求性质、缺陷等级、版本、计划时间筛选。
4. 支持查看“为什么这个任务排在前面”的计算说明。
5. 人工调整排期时必须填写调整原因，并保留操作日志。
6. 新建开发任务时，应提供所选负责人排期甘特图入口；甘特图默认展示待处理/处理中的开发任务和缺陷修复，支持用户通过多选下拉按状态切换展示范围，并支持关键词搜索事项、项目、需求和任务。
7. 甘特图应按优先级分数倒序排列，并用左侧固定信息表清晰展示事项名称、类型、状态、起止时间和优先级分数；未设置计划时间的事项也进入同一表体参与排序，右侧时间轴区域显示未排期提示。所属项目、关联需求、关联任务作为事项摘要和悬浮详情展示。开发任务展示关联需求，缺陷修复展示关联需求和关联任务，系统编号不作为甘特图核心展示字段。
8. 甘特图排期条支持通过左右边界拉伸调整计划起止时间，也支持直接编辑起止日期；任一方式调整后都应立即刷新时间轴位置，点击保存后批量更新对应开发任务或缺陷修复的计划时间。仅待处理/处理中的开发任务、待修复/修复中的缺陷允许编辑排期，其他状态只读展示。排期编辑需遵守所属项目已维护的计划开始/结束边界；未维护的项目计划边界不限制排期。
9. 甘特图左侧信息表支持拖拽调整宽度，用户可根据任务名称长度和时间轴范围自行平衡阅读空间。
10. 新建开发任务表单中的当前任务作为高亮临时事项进入甘特图，并按优先级分数参与统一排序；保存临时事项排期只回写表单计划开始/完成时间，不创建正式数据。
11. 甘特图排期调整权限限定为负责人本人，登录人查看他人排期时不可编辑。

## 8. 页面与导航建议

### 8.1 首页/工作台

登录后默认进入个人工作台。工作台对所有人员保持一致的信息架构，通过二级导航承载个人负责事项和个人排期；岗位只影响按钮显隐和可操作范围，不改变页面结构。

#### 8.1.1 工作台二级导航

| 页签 | 说明 |
| --- | --- |
| 我负责的事项 | 将当前人员作为开发/修复负责人或测试负责人的开发任务、缺陷修复合并在一个表格中展示 |
| 排期 | 内联展示当前人员负责的开发任务和缺陷修复排期表及甘特图，支持关键词搜索、状态筛选、日期编辑和甘特图边界拖拽 |

工作台默认打开“我负责的事项”，不设置概览页，不展示说明性文案、快捷入口板块、最近事项或后台处理记录。后台处理记录只在后台管理的处理记录页展示。

#### 8.1.2 我负责的事项规则

1. 事项范围包含当前人员作为开发任务负责人、缺陷修复负责人、开发任务测试负责人或缺陷测试负责人的事项。
2. 开发任务和缺陷修复在同一张表格展示，字段至少包含事项、类型、状态、项目、负责人、测试负责人、排期、时间状态、优先级分数、创建人和创建时间。
3. 表格支持关键词搜索、枚举筛选、表头排序、分页、固定操作列和列宽拖拽。
4. 支持临期/超期筛选。用户可设置临期阈值，临期阈值和时间状态筛选选择后即时应用到表格数据，不再设置独立“筛选”按钮。
5. 临期和超期仅针对未完成事项判断；已完成、已验证或已关闭事项不参与临期/超期高亮。临期和超期事项通过“时间状态”标签和浅色整行背景高亮展示，不使用无说明的行首色块；超期优先级高于临期。
6. 事项状态筛选采用层级结构展示：开发任务状态和缺陷状态分组展示；筛选值应包含事项类型前缀，避免开发任务“已关闭”和缺陷“已关闭”等同名状态互相混淆。
7. 默认排序规则：未完成事项排在已完成事项前；未完成事项按优先级分数倒序，分数相同时按计划完成时间升序；已完成事项按完成或最近状态更新时间倒序。
8. 未完成/已完成映射：开发任务的测试通过、已关闭视为已完成，其余待处理、处理中、待测试、测试中视为未完成；缺陷的已验证、已关闭视为已完成，其余待修复、修复中、已修复视为未完成。
9. 工作台操作列应包含当前事项可发起的主要动作。权限不满足时按钮隐藏；仅业务状态不满足时按钮展示为禁用态并给出说明。
10. 开发任务操作：任务负责人可调整排期、开始处理、处理完成、手动关闭；排期和手动关闭仅待处理/处理中可用。产品经理或测试可开始测试、测试通过；测试通过需任务下缺陷全部已验证或已关闭。产品经理或测试可在测试中/测试通过任务上创建缺陷。
11. 缺陷操作：缺陷负责人可调整排期、开始修复、标记已修复；排期仅待修复/修复中可用。产品经理或测试可验证通过、验证未通过、关闭、开启；关闭仅待修复/修复中可用，开启仅已关闭可用。

#### 8.1.3 排期规则

1. 排期页复用统一排期组件，默认展示当前人员作为开发任务负责人或缺陷修复负责人的事项。
2. 排期页和新建开发任务表单中的“查看排期情况”复用同一个排期组件，规则保持一致。
3. 甘特图默认按优先级分数倒序排列，搜索能力复用统一表格搜索组件，支持按事项、编号、所属项目、关联需求、关联任务、计划开始和计划完成字段查询；同时支持状态多选筛选、直接编辑起止日期、左右边界拖拽调整和批量保存。保存排期时，系统按所属项目已填写的计划开始/结束时间进行边界校验。
4. 未排期事项也进入同一表体参与展示和排序，不再单独放到下方列表；未排期事项右侧时间轴区域显示未排期提示。
5. 排期调整权限仍限定为负责人本人；当前人员仅作为测试负责人但不是开发/修复负责人时，该事项不计入个人排期，仅在“我负责的事项”中查看。
6. 只有待处理/处理中的开发任务、待修复/修复中的缺陷允许调整排期。

如人员存在多个岗位，工作台合并展示所有分配给该人员的负责事项；岗位只控制具体按钮是否展示或是否允许操作。

### 8.2 项目管理

项目管理参考成熟研发管理软件的经典结构，但不引入产品管理概念。左侧一级导航进入项目管理后，页面顶部先展示项目上下文，并在项目内提供横向业务导航。

项目内顶部导航：

| 页签 | 说明 |
| --- | --- |
| 概览 | 展示项目基本信息、状态、负责人、计划周期、需求/任务/缺陷/版本统计、项目背景、目标和涉及系统 |
| 需求 | 展示项目下所有需求，按筛选条件进行管理，任务创建入口保留在需求行内 |
| 任务 | 展示项目下所有开发任务，支持字段查询、任务状态/类型筛选、表头排序、分页、排期、状态推进、开发详情和从任务行内创建缺陷 |
| 缺陷 | 展示项目下所有缺陷，缺陷实际仍关联任务，但在项目缺陷页统一查询、处理和查看详情 |
| 版本 | 展示项目版本，支持版本规划、发布检查和发布操作 |
| 资料 | 展示项目资料描述和附件，支持新增资料 |

项目管理业务规则：

1. 上方项目上下文支持切换项目，展示项目阶段、项目编号、负责人、计划周期、期望上线时间、需求范围等信息。
2. 项目内不强制使用树结构。需求、任务、缺陷分开管理：需求页负责范围和评审，任务页负责执行和排期，缺陷页负责质量问题查询和闭环。
3. 需求页提供“需求状态”和“上线状态”两个独立筛选项，均为可多选下拉；筛选项为空时展示全部需求，不用平铺标签按钮承载筛选。
4. 任务页提供“任务状态”筛选项，为可多选下拉；筛选项为空时展示全部任务。
5. 缺陷页提供“缺陷状态”“发现环境”“缺陷等级”三个独立筛选项，均为可多选下拉；筛选项为空时展示全部缺陷。
6. 创建任务入口放在需求行内，创建缺陷入口放在任务行内，避免缺陷缺少明确任务上下文。
7. 项目缺陷页可统一查看和处理缺陷，但默认不作为新建缺陷入口。
8. 在项目管理内新建需求、资料、版本时，所属项目从当前选中项目自动带出。
9. 在需求行内创建任务时，所属项目和关联需求自动带出并只读展示。
10. 在任务行内创建缺陷时，所属项目、关联需求和关联任务自动带出并只读展示。
11. 需求详情、开发详情、缺陷详情采用只读侧拉窗展示，不复用创建/编辑表单组件；用户可点击侧拉窗外区域或关闭按钮退出详情。
12. 项目概览用于看状态和风险，需求页用于管范围，任务页用于管执行，缺陷页用于管质量，版本页用于管上线，资料页用于管沉淀。

### 8.3 后台管理

后台管理仅允许岗位为产品经理的人员进入，用于维护系统基础数据和配置。

后台管理采用分区标签承载配置，不再将各配置项平铺为长页面：

1. 人员账号：统一维护人员基础信息、组织归属、岗位、登录账号、账号状态和初始密码。
2. 组织岗位：维护组织架构和岗位字典，岗位包含在人员管理范围内，不单独作为一级页面。
3. 字典配置：维护业务下拉项、展示名称、启停状态和排序。
4. 优先级规则：维护需求优先级分值、缺陷基础分值及相关计算配置。
5. 看板规则：维护全局看板使用的临期阈值、人员负载阈值、项目停滞天数、高优先级阈值和是否统计已关闭事项。
6. 处理记录：集中展示后台配置变更、账号管理和关键业务操作日志。

岗位管理包含在人员管理内，不单独建设岗位管理页面。

字典配置支持新增、编辑显示名称、停用和排序。项目阶段、需求状态、任务状态、缺陷状态、版本状态、需求性质、缺陷等级等内置关键字典保留系统编码；可修改显示名称，但不允许删除或修改系统编码，以保证状态流转、优先级计算和发布拦截规则稳定运行。

看板规则配置字段：

| 字段 | 默认值 | 说明 |
| --- | --- | --- |
| 临期阈值 | 2 天 | 计划完成时间距离当前日期小于等于该天数且未完成/未关闭时，计为临期 |
| 人员正常负载上限 | 5 项 | 人员负责未完成事项数不超过该值时，负载为正常 |
| 人员饱和负载上限 | 10 项 | 超过正常上限但不超过该值时，负载为饱和；超过该值时，负载为超载；超期事项作为风险和超期指标单独展示，不直接把人员负载改为超载 |
| 项目停滞天数 | 7 天 | 项目相关未完成事项超过该天数无更新时，可被识别为停滞 |
| 高优先级阈值 | 40 | 未完成事项优先级分数大于等于该值时，计为高优先级事项 |
| 是否统计已关闭事项 | 否 | 控制看板总览和图表默认是否纳入已关闭事项；用户选择已关闭范围时仍可查看关闭数据 |

### 8.4 全局看板

全局看板用于产品经理从整体视角掌握项目、人员、任务和风险情况。它不替代个人工作台；工作台关注“我负责什么”，全局看板关注“整体交付是否健康”。

入口规则：

1. 全局看板作为一级导航，仅岗位为产品经理的人员可见。
2. 其他岗位不展示入口，后端统计相关配置接口也按产品经理岗位校验。
3. 看板数据从项目、需求、开发任务、缺陷、人员和后台看板规则配置聚合，不要求用户手工维护统计结果。

全局筛选：

| 筛选项 | 说明 |
| --- | --- |
| 项目 | 支持多选，默认全部项目 |
| 计划时间范围 | 按事项计划完成时间筛选 |
| 事项类型 | 全部、开发任务、缺陷修复 |
| 状态范围 | 全部、未完成、已完成、已关闭 |
| 负责人 | 按开发任务负责人或缺陷修复负责人筛选，支持搜索 |
| 测试负责人 | 按开发任务/缺陷测试负责人筛选，支持搜索 |
| 岗位 | 按负责人或测试负责人的主岗位筛选 |

#### 8.4.1 总览

总览展示整体指标和图表。

核心指标均按当前全局筛选范围统计，并展示“当前数量/总数”：

1. 进行中项目/项目总数。
2. 未完成需求/需求总数。
3. 未完成开发任务/开发任务总数。
4. 未闭环缺陷/缺陷总数。
5. 临期事项/事项总数。
6. 超期事项/事项总数。
7. 超载人员/人员总数。
8. 高优先级未完成事项/未完成事项总数。

图表：

1. 项目健康分布。
2. 开发任务状态分布。
3. 缺陷状态分布。
4. 岗位工作量分布。
5. 近 14 天完成趋势。

#### 8.4.2 项目视角

项目视角以项目为主线展示每个项目的交付健康度。

项目视角表格操作列提供“查看排期”：

1. 每个项目行展示“查看排期”按钮，点击后打开该项目的项目甘特图，不直接在项目视角页面平铺展示。
2. 项目甘特图复用统一排期甘特图组件，展示当前全局筛选范围内且属于该项目的开发任务和缺陷修复。
3. 项目甘特图第一行固定展示项目计划周期，取项目计划开始时间和计划结束时间，用于和下方任务/缺陷排期进行对照。
4. 甘特图默认跟随全局计划时间、事项类型、状态范围、负责人、测试负责人和岗位筛选；甘特图内部状态筛选默认不选中任何值，空值表示展示全部状态，用户可再通过事项搜索和状态多选做二次缩小范围。
5. 项目甘特图用于产品经理总览单个项目排期，此处只读查看，不直接调整排期；排期调整仍遵循“负责人本人才能调整自己名下待处理/处理中的开发任务、待修复/修复中的缺陷”的统一规则。
6. 项目甘特图左侧信息表不展示起止日期输入框，改为展示负责人；搜索字段同步以事项、编号、所属项目、关联需求、关联任务和负责人为主，不提供计划开始/计划完成日期搜索字段。

表格字段至少包含：项目名称、项目状态、项目负责人、计划周期、需求数、开发任务数、缺陷数、未完成事项数、临期数、超期数、最高优先级分数、健康状态、最近更新时间和操作。

项目健康状态：

| 状态 | 规则 |
| --- | --- |
| 正常 | 无超期事项，无阻塞或严重未闭环缺陷 |
| 关注 | 存在临期事项，或存在严重未闭环缺陷 |
| 风险 | 存在超期事项，或存在阻塞未闭环缺陷 |
| 停滞 | 项目相关未完成事项超过配置天数无更新，且没有更高优先级风险 |

操作：

1. 查看项目：跳转项目管理并选中对应项目。
2. 项目视角表格支持表头排序、分页、横向滚动和右侧固定操作列。

#### 8.4.3 人员视角

人员视角以员工为主线，分别统计“作为开发/修复负责人”和“作为测试负责人”的事项。

表格字段至少包含：姓名、岗位、组织、作为开发负责人的未完成事项数、作为测试负责人的未完成事项数、开发任务数、缺陷修复数、待测试/待验证数、临期数、超期数、最高优先级分数、负载状态、最近完成时间和操作。

负载状态：

| 状态 | 规则 |
| --- | --- |
| 空闲 | 未完成事项数为 0 |
| 正常 | 未完成事项数大于 0 且不超过正常负载上限 |
| 饱和 | 未完成事项数超过正常负载上限且不超过饱和负载上限 |
| 超载 | 未完成事项数超过饱和负载上限 |

人员排期：

1. 人员视角可查看某个人的排期，复用统一排期甘特图组件。
2. 查看他人排期时只读；只有登录人等于事项负责人时，才允许保存排期变更。

#### 8.4.4 风险事项

风险事项聚合所有需要产品经理关注的开发任务和缺陷修复。

默认纳入风险的事项：

1. 临期事项。
2. 超期事项。
3. 阻塞或严重未闭环缺陷。
4. 高优先级未完成事项。
5. 长时间未更新事项。

表格字段至少包含：事项标题、事项类型、所属项目、关联需求、关联任务、负责人、测试负责人、状态、计划完成时间、优先级分数、风险原因、创建时间、最近更新时间和操作。

操作：

1. 查看详情：开发任务打开开发详情，缺陷打开缺陷详情。
2. 跳转项目：进入项目管理并选中所属项目。

### 8.5 全局列表页

本期不建设独立的全局需求池或全局缺陷池一级导航。跨项目事项通过以下位置承接：

1. 工作台：承接当前登录人员的需求开发、缺陷修复、待测试、待验证和临期超期事项。
2. 全局看板：承接产品经理视角下的项目健康、人员负载和风险事项。
3. 项目管理：承接项目内需求、任务、缺陷、版本、资料和项目概要。
4. 后台管理：承接组织人员、登录账号、字典、优先级规则、看板规则和处理记录。

后续如需要全局需求池、全局缺陷池或更完整的效能分析，应基于实际使用频率再扩展为独立模块。

## 9. 处理提醒

本期暂不建设系统通知功能，也不做企业微信、飞书、邮件等外部消息集成。临期、超期、高优先级、发布拦截等提醒先通过工作台、全局看板、列表标识和发布检查结果展示。

第一版重点展示以下提醒：

1. 工作台中展示当前人员的临期和超期事项。
2. 全局看板中展示产品经理视角的临期、超期、高优先级、长时间未更新和阻塞/严重缺陷风险。
3. 任务和缺陷列表中标识临期、超期和高优先级事项。
4. 版本发布前展示发布检查结果，若存在非已完成需求、上线状态不是待上线的需求，或非已验证/已关闭缺陷，则拦截发布并展示原因。
5. 后台管理保留处理记录，用于追溯配置变更、账号管理和关键业务操作；其他业务页面不展示处理记录模块。

## 10. 查询、筛选与统计

### 10.1 查询筛选

系统应支持按以下条件查询：

1. 项目名称、项目编号、负责人、阶段、计划开始时间、计划结束时间、期望上线时间、实际开始时间、实际结束时间。
2. 需求编号、需求标题、需求类型、需求状态、上线状态、需求性质、评审日期。
3. 任务类型、任务状态、负责人、计划开始/完成时间、优先级分数。
4. 缺陷等级、缺陷状态、负责人、发现环境、关联任务、间接关联需求、关联版本。
5. 版本名称、版本状态、计划发版时间、实际发版时间。

列表筛选交互规则：

1. 本系统所说的表格组件包括表体、搜索、筛选、排序、分页、工具栏、固定列等完整列表能力集合；表格能力优先从 Ant Design 或成熟组件库中选型，不自研复杂表格周边组件。
2. 表格顶部搜索区只用于字符串字段和时间字段：字符串字段使用关键词查询，时间字段使用日期区间查询；不采用“输入后扫描所有字段”的粗查询方式。
3. 搜索功能必须允许用户选择查询字段，例如编号、标题、项目、需求、任务、负责人、创建人、创建时间、计划开始时间、计划完成时间等。
4. 筛选组件用于枚举值字段，例如需求状态、上线状态、任务状态、任务类型、缺陷状态、缺陷等级、发现环境、是否启用等，支持单选或多选。
5. 筛选项默认空值表示不过滤，即展示当前范围内全部数据。
6. 不同业务含义的状态必须拆开，例如需求状态和上线状态是两个独立筛选项。
7. 业务列表统一采用企业级表格能力：字段查询、日期区间查询、枚举筛选、表头正序/倒序排序、分页器和横向滚动。
8. 表格能力优先复用 Ant Design Table、Input.Search、DatePicker.RangePicker、Select、Pagination 等成熟组件；复杂列表可评估 ProTable 等成熟高级表格组件。
9. 不允许把枚举值字段放入搜索范围，例如状态、类型、缺陷等级应放在筛选组件中，不作为关键词搜索字段。
10. 需求、任务、缺陷、版本、资料、人员、组织、岗位、字典和优先级规则等主要表格均需展示创建人或系统可识别的创建来源，以及创建时间；历史无创建人的配置数据允许展示为空。
11. 表格默认分页展示，支持用户切换每页 10、20、50 条；分页、筛选和排序应保持在当前业务范围内生效，例如当前项目、当前负责人或当前后台配置分类。
12. 含操作按钮的表格应将“操作”列固定在右侧，横向滚动查看字段时，详情、编辑、排期、状态流转等按钮始终可见。
13. 表格操作列按钮超过 3 个时，只保留前 3 个高频按钮直接展示，其余按钮统一收起到“更多（...）”菜单中；该规则适用于所有业务表格。
14. 排期甘特图的左侧事项信息表作为可复用组件，同时用于工作台排期页和新建任务表单的查看排期弹窗；该组件的搜索能力复用统一表格搜索组件，并保留状态筛选、优先级排序、未排期事项并入表体、直接编辑日期、左右边界拖拽调整和负责人本人才能保存排期的规则。

### 10.2 页面内统计

第一版不建设独立效能度量一级模块；统计信息通过工作台、全局看板和项目管理内嵌展示：

1. 工作台展示个人需求开发、缺陷修复、临期/超期数量。
2. 全局看板展示产品经理视角的项目健康、人员负载、风险事项和完成趋势。
3. 项目管理展示当前项目的需求、未完成任务、未闭环缺陷、版本数量。
4. 待测试任务、测试中任务、待验证缺陷和线上缺陷通过工作台、全局看板和项目缺陷视图承接，不建设独立测试管理一级模块。

## 11. 数据安全与操作留痕

1. 用户必须登录后使用系统。
2. 本期不建设完整 RBAC、数据权限或可配置页面权限体系；关键入口和按钮采用岗位代码写死的轻量权限控制，并由后端接口同步校验。
3. 登录账号的创建、停用、锁定、解锁、重置密码、绑定人员变更应记录操作日志。
4. 密码不得明文存储，账号登录失败、账号锁定、密码重置等关键安全事件应保留记录。
5. 需求、任务、缺陷、版本的关键状态变更应记录操作日志。
6. 优先级分值配置和变更应记录操作人、操作时间和变更前后内容。
7. 人工调整优先级或排期必须填写原因。
8. 删除项目、需求、任务、缺陷、版本等关键数据应采用逻辑删除。
9. 系统应保留数据变更历史，便于追溯。
10. 项目资料应记录上传人、更新时间和历史版本，避免资料被覆盖后不可追溯。

## 12. 非功能需求

| 类型 | 要求 |
| --- | --- |
| 易用性 | 常用列表支持筛选、排序、批量操作和快捷查看 |
| 性能 | 常规列表查询 3 秒内返回，支持分页加载 |
| 可追溯 | 关键对象需记录创建、修改、状态流转和操作人 |
| 可配置 | 字典、优先级分值、组织架构、岗位、登录账号状态应可配置 |
| 安全性 | 项目资料和人员信息需具备基础数据保护和操作留痕 |
| 扩展性 | 后续可接入企业 IM、代码仓库、CI/CD、测试平台 |
| 可靠性 | 重要操作需二次确认，发布和规则变更需保留日志 |

### 12.1 技术实现建议

| 层级 | 技术方案 |
| --- | --- |
| 前端 | React + TypeScript + Ant Design |
| 后端 | Node.js + NestJS |
| 数据库 | MySQL |
| ORM | Prisma |
| 登录 | 账号密码登录，密码加密存储 |
| 部署形态 | 第一版按内网 Web 系统建设，后续可按实际环境扩展 |

前后端均采用 TypeScript，有利于复用类型定义和降低后续维护成本。前端采用 Ant Design 作为企业级中后台设计体系，基础布局、表格、卡片、按钮、标签、选择器等通用控件应优先复用该体系，再结合内部需求管理业务进行定制。大段说明类字段统一使用长文本输入，不提供富文本工具栏。全局看板图表采用 Ant Design Charts，并按需懒加载，避免普通工作台和项目管理页面承担不必要的图表首屏资源。后端采用 NestJS 分模块组织项目、需求、任务、缺陷、版本、组织人员、登录账号和后台配置。

## 13. 验收标准

### 13.1 项目管理

1. 用户可创建项目并填写需求范围、计划开始时间、计划结束时间、期望上线时间和项目负责人；计划开始时间默认当天；实际开始时间和实际结束时间由启动、结项、重新打开等项目生命周期操作自动维护。
2. 项目阶段包含已立项、进行中、上线运维、已结项。
3. 一个项目可关联多个需求、多个版本、多个资料和多个成员。
4. 项目管理页上方可选择项目并展示项目概要，概要中必须明确展示项目状态；项目内提供概览、需求、任务、缺陷、版本、资料等顶部页签。
5. 新建项目时当前阶段固定为已立项，创建和编辑项目表单不展示阶段字段，不允许手动选择阶段。
6. 项目负责人只能从产品经理岗位人员中选择。
7. 项目创建后支持产品经理或项目负责人编辑项目信息，项目负责人仍只能选择产品经理岗位人员。
8. 需求范围、项目背景、项目目标、涉及系统支持长文本录入并保留换行展示。
9. 新建项目提交成功后，系统应立即进入该项目的项目管理页面并选中新建项目。

### 13.2 资料归档

1. 用户可在项目下维护业务和技术资料。
2. 新增资料表单包含资料名称、资料类型、资料描述和上传附件；资料描述使用长文本输入，上传附件支持选择本地文件，由系统上传并保存附件地址，单个附件限制 50MB。
3. 资料可分类、搜索、预览、下载、更新版本。
4. 资料可关联需求和版本。

### 13.3 需求管理

1. 产品经理可在项目下创建需求，所属项目自动带出并只读展示。
2. 需求包含需求类型、需求状态、上线状态、需求性质、时效加分、评审日期、评审结论等字段；创建需求表单需实时展示只读优先级总分和计算逻辑。
3. 新建需求表单暂不展示负责人字段。
4. 产品经理可在需求表格行内填写评审结果，评审结论自动推动需求状态。
5. 需求评审通过或开发中时，可在需求表格行内拆解开发任务。
6. 需求优先级可按规则自动计算。
7. 需求发生变更或优化时，可创建需求变更记录并保留变更前后内容。
8. 产品经理可触发需求变更或需求优化，原需求进入终态，原需求下任务和缺陷关闭，并创建关联原需求的新需求。
9. 需求变更或需求优化创建的新需求，应按变更/优化类型和上线状态叠加场景加分。
10. 需求描述和验收标准使用统一长文本组件。
11. 创建需求时可从当前项目资料库中选择已上传的业务资料，并自动关联到该需求；不在需求表单中直接上传或新建资料。
12. 需求类型包含非功能需求，用于技术优化、架构治理、性能稳定性、代码质量等无直接业务需求场景。
13. 需求列表提供查看详情入口，详情页展示基础信息、需求内容、评审信息、验收信息、关联资料，并以标签页展示关联任务和关联缺陷。
14. 当需求为开发中且所有关联任务均为测试通过时，产品经理可点击验收完成，填写产品经理验收结论；存在 UI 任务时还需填写 UI 验收结论，提交后需求进入已完成。
15. 需求筛选中的开发中只筛选开发中状态，评审通过作为独立状态筛选。

### 13.4 开发任务

1. 产品经理、UI、前端、后端、数据、运维等岗位人员可在需求表格行内创建任务；测试岗位不展示创建开发任务入口。
2. 创建任务时，所属项目和关联需求自动带出并只读展示。
3. 创建任务时，负责人默认当前登录人且允许修改；任务负责人必填，不能为空；任务类型根据负责人主岗位自动带出并只读展示；负责人未配置岗位时不能提交，不允许使用默认岗位兜底。
4. 如果 A 创建负责人为 B 的任务，B 登录后应在个人工作台看到该任务。
5. 创建任务时可选择测试负责人，候选人员限定为测试岗位人员，默认不自动带出；测试负责人必填，不能为空；历史任务测试负责人为空时，任何人都不能执行开始测试或测试通过；测试负责人登录后可在工作台“我负责的事项”看到该任务。
6. 任务支持维护计划开始时间、计划完成时间、实际时间、状态和负责人。
7. 任务支持待处理、处理中、待测试、测试中、测试通过、已关闭状态流转。
8. 任务测试通过前，任务下缺陷必须全部已验证或已关闭，或无缺陷记录。
9. 被分配的缺陷可在开发任务视图中展示。
10. 开发人员点击处理完成时弹出开发完成表单，必须填写开发说明；可从当前项目资料库中选择已上传的技术资料并关联到该开发任务，不在开发完成表单中直接上传或新建资料。
11. 开发任务列表提供查看详情入口，详情展示基础信息、排期执行、开发说明、关联资料和缺陷概况，不复用创建/编辑表单组件。

### 13.5 缺陷记录

1. 产品经理或测试可在任务表格行内创建缺陷并填写缺陷标题、缺陷等级、发现时间、发现入口、影响范围、前置条件、缺陷描述、复现步骤、实际结果、期望结果、设备/浏览器/客户端版本、测试账号/样例数据、附件链接和时效加分说明；开发岗位不展示创建缺陷入口；缺陷描述使用长文本输入，发现环境由系统自动判断且不在表单展示，表单需实时展示只读优先级分数和计算逻辑。
2. 创建缺陷时，所属项目、关联需求和关联任务自动带出并只读展示。
3. 缺陷负责人默认继承关联任务负责人，创建时不手动选择；若关联任务负责人为空，则禁止创建缺陷；优先级通过任务追溯需求性质计算。
4. 缺陷测试负责人默认当前登录人，可修改为产品经理或测试岗位人员；缺陷测试负责人必填，不能为空；历史缺陷测试负责人为空时，任何人都不能执行验证、关闭或开启；测试负责人登录后可在工作台“我负责的事项”看到该缺陷。
5. 缺陷可进入待修复、修复中、已修复、已验证、已关闭流程。
6. 产品经理或测试可验证通过、验证未通过、关闭和开启缺陷；不满足状态条件时按钮置为禁用态，其中关闭仅允许在待修复或修复中状态操作，开启仅允许在已关闭状态操作。
7. 缺陷优先级可按缺陷等级、环境、归属需求系数和时效加分自动计算。
8. 缺陷列表提供查看详情入口，详情展示创建人、创建时间、归属项目、关联需求、关联任务、修复排期、实际开始修复时间、实际完成修复时间、现象影响、复现定位、环境和附件。

### 13.6 版本管理

1. 产品经理或测试可创建版本并通过弹窗表格多选本次上线需求。
2. 产品经理或测试可通过弹窗表格多选本次修复缺陷。
3. 版本只能选择当前项目下已完成且待上线的需求，以及当前项目下已验证或已关闭的缺陷。
4. 版本发布后可保留上线范围快照、发布结果和实际发版时间。
5. 版本内存在非已完成需求时，发布被拦截。
6. 版本内存在上线状态不是待上线的需求时，发布被拦截。
7. 版本内存在非已验证/已关闭缺陷时，发布被拦截。
8. 创建、编辑和发布版本操作仅允许岗位为产品经理或测试的人员执行。

### 13.7 组织人员与岗位

1. 可维护组织架构、部门、小组、人员、岗位、登录账号和项目成员。
2. 人员可维护员工编号、联系方式，并关联所属组织、所属部门和岗位。
3. 岗位可用于任务分工、负责人筛选和统计展示。
4. 可为人员创建、绑定、停用、锁定、解锁和重置登录账号。
5. 登录账号可用于系统登录，但账号本身不作为权限控制依据；权限依据绑定人员岗位判断。
6. 后台管理入口仅允许岗位为产品经理的人员进入，关键按钮按“权限不满足隐藏、功能条件不满足禁用”的规则展示。

### 13.8 表单交互

1. 新增和编辑类表单采用居中弹窗展示，并支持暂存草稿、清除草稿和正式提交。
2. 暂存草稿不触发正式业务创建或状态流转，提交成功后自动清除对应草稿。
3. 必填字段在前端有明确标识和校验，不要求数据库层创建非空约束。
4. 不可编辑字段采用只读样式，并可随表单提交对应隐藏 ID。
5. 项目、需求、任务、人员、组织、岗位等业务对象选择字段支持输入关键词查询，不只能通过下拉滚动选择。

## 14. 待确认事项

1. 正式部署环境的 MySQL 地址、数据库名和初始产品经理账号。
2. 第一版是否需要需求父子层级；若不确认，默认先使用平铺需求，较大优化通过关联原需求处理。
3. 版本号命名规范是否由系统自动生成；若不确认，默认手动填写并校验项目内唯一。
4. 第一版是否需要导入历史项目、需求、缺陷数据。
5. 项目结项后是否需要升级为全量冻结规则：除重新打开和资料维护外，是否禁止已存在需求、任务、缺陷、版本继续编辑或状态流转。当前版本暂不实现该冻结，仅冻结新增和发布类入口。

## 15. 附录：核心数据关系

```text
项目 1 - N 项目资料
项目 1 - N 需求
项目 1 - N 版本
项目 N - N 人员
人员 1 - 0/1 登录账号
需求 1 - N 需求变更记录
需求 1 - N 关联优化需求
需求 1 - N 开发任务
任务 1 - N 缺陷
版本 N - N 需求
版本 N - N 缺陷
人员 1 - N 开发任务
人员 1 - N 缺陷
```

## 16. 附录：优先级示例

示例 1：P1 需求的新需求开发任务，未配置时效加分。

```text
优先级分数 = 30 + 0 = 30
```

示例 2：P0 需求下的线上 2 级严重缺陷，未配置时效加分。

```text
优先级分数 = 45 x 1.20 + 0 = 54
```

示例 3：P2 需求下的测试环境阻塞缺陷，时效加分 10。

```text
优先级分数 = 40 x 1.10 + 10 = 54
```

示例 4：P2 已上线需求变更，未配置时效加分。

```text
优先级分数 = 20 + 15 + 0 = 35
```
