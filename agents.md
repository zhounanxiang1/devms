# Agents Guide

本文件给后续参与本项目的开发助手使用。请先读这里，再读 PRD 和代码。

## 项目定位

这是一个内部需求开发管理系统，目标是管理项目、需求、开发任务、缺陷、版本、资料、组织人员和后台配置。系统面向企业内部协作，不是营销站点，也不是轻量 demo。

PRD 是业务规则源头：`内部需求开发管理系统_PRD.md`。任何业务规则、状态流转、表单字段、优先级算法或交互规则变化，都必须同步更新 PRD 和代码。

## 技术栈

- 前端：React + TypeScript + Vite。
- UI 组件：Ant Design，当前项目使用 `antd@6.5.1`。
- 后端：Node.js + NestJS。
- 数据库：MySQL。
- ORM：Prisma。
- 默认前端地址通常为 `http://localhost:5174`，后端 API 通常为 `http://localhost:4000/api`。

常用脚本：

```bash
npm install
npm run dev
npm run dev:web
npm run dev:api
npm run build --workspace apps/web
npm run build --workspace apps/api
npm run db:generate
npm run db:migrate
npm run db:seed
```

## 前端组件硬规则

以后所有前端组件，只能优先从 Ant Design 体系中找，不要自行发挥。

- 本项目所说的“表格组件”不是单个表体，而是表体、搜索、筛选、排序、分页、工具栏、列设置等与表格整体功能相关的组件集合。
- 表体优先使用 Ant Design Table。
- 表格搜索、筛选、排序、分页、固定列等能力优先使用 Ant Design 体系能力。
- 搜索组件只用于字符串字段和时间字段：字符串字段使用 Ant Design `Input.Search`，时间字段使用 Ant Design `DatePicker.RangePicker` 做区间查询。
- 筛选组件用于枚举值字段，例如状态、上线状态、类型、等级、环境、是否启用等，优先使用 Ant Design `Select` 多选或 Table filters。
- 不要把枚举值字段放进“搜索范围”，例如状态、类型、缺陷等级不应出现在关键词搜索字段里。
- 如果业务需要完整高级表格集合，先评估 Ant Design ProComponents ProTable 或其他成熟组件库，再结合业务开发。
- 不允许自行设计和手写复杂表格周边组件，例如自定义搜索栏、自定义筛选面板、自定义排序面板、自定义表格工具栏。
- 弹窗、抽屉、确认框：使用 Ant Design Modal、Drawer、Popconfirm 等。
- 表单、输入框、日期、选择器、上传：使用 Ant Design Form、Input、DatePicker、Select、Upload 等。
- 标签、状态、按钮、卡片、描述列表：使用 Ant Design Tag、Button、Card、Descriptions 等。
- 不要手写日期选择器、下拉选择器等已有成熟组件。
- Ant Design 没有合适组件时，先向用户确认，再做轻量封装。
- 不要为了安装高级组件强行解决 peer dependency 冲突。例如 ProTable 若与当前 AntD 版本不兼容，不要 `--force` 安装。

设计取向：参考 PingCode 一类成熟企业级研发管理工具，强调清晰、稳定、密度合理、操作路径短。不要做花哨营销式 UI。

## 代码工作规则

- 不要回滚用户或其他助手已经做过的改动，除非用户明确要求。
- 手动编辑文件使用 `apply_patch`。
- 搜索文件和内容优先使用 `rg`。
- 修改业务逻辑后要跑相关构建或校验，至少保证前端/后端能编译。
- 有数据库模型变化时，要同步 Prisma schema、迁移、后端读写逻辑、前端类型和 PRD。
- 前端表单里的必填校验优先做前端控制，不要随意给数据库加非空约束。
- 表单支持暂存草稿；点击提交才正式提交。
- 所有表格默认应具备字段搜索、枚举筛选、列头排序、分页、横向滚动和固定操作列。

## 核心业务规则

### 组织与账号

- 当前没有“用户角色”概念，不做复杂 RBAC。
- 系统维护组织架构、人员、岗位和登录账号。
- 岗位用于按钮控制、任务类型、负责人筛选和统计展示。
- 登录为简单账号密码登录。
- 新建人员的初始密码为 `123`。
- 后台管理入口按岗位控制，只允许产品经理进入。

### 项目

- 项目是一组持续迭代需求的集合，不是一次发版后就结束。
- 项目可以有多个版本。
- 项目负责人是项目上的角色字段，不是岗位。
- 项目负责人只能从产品经理岗位人员中选择。
- 新建项目默认状态为 `INITIATED`，创建/编辑表单不展示当前阶段。
- 计划开始时间默认当天；项目保留计划开始时间、计划结束时间、期望上线时间。
- 项目创建后可以编辑信息。允许项目负责人编辑项目。
- 项目状态保持大颗粒度：`INITIATED`、`IN_PROGRESS`、`ONLINE_OPS`、`CLOSED`。
- 项目启动从已立项进入进行中；结项进入已结项；已结项可重新打开。

### 需求

- 需求属于项目。
- 新建需求不需要负责人字段。
- 需求描述和验收标准支持富文本。
- 需求可上传资料，资料同时关联到需求。
- 创建需求时上线状态默认为待上线；需求变更/优化创建新需求时允许选择上线状态。
- 需求状态：`TO_REVIEW`、`APPROVED`、`REJECTED`、`NEEDS_SUPPLEMENT`、`DEFERRED`、`DEVELOPING`、`COMPLETED`、`CANCELED`、`CHANGE`、`OPTIMIZATION`。
- 上线状态单独维护：`TO_RELEASE`、`RELEASED`。
- 产品经理填写评审结果，评审结论自动推动需求状态；状态通常不允许手动改。
- 只有评审通过或开发中的需求可以创建开发任务。
- 需求创建任务后，需求从评审通过进入开发中。
- 需求下所有任务测试通过后，需求从开发中进入已完成。
- 版本发布关联需求后，需求上线状态从待上线变为已上线。
- 产品经理任何情况下可发起需求变更或需求优化；原需求状态变为需求变更或需求优化，原需求下所有任务和缺陷关闭，同时创建一个关联原需求的新需求。
- 需求变更/需求优化视为终态，后续不允许继续操作。

### 开发任务

- 任务只能从需求表格行内创建。
- 任务创建时，所属项目和关联需求由系统自动带出，不可编辑。
- 新建任务时负责人默认当前登录用户，但允许手动改负责人。
- 任务类型由负责人岗位自动带出，不可编辑。
- 任务创建初始状态为 `TODO`。
- 任务状态流转：`TODO -> DOING -> TO_TEST -> TESTING -> TEST_PASSED / CLOSED`。
- 负责人点击开始处理：待处理变处理中，并自动填实际开始时间。
- 负责人点击处理完成：处理中变待测试，并自动填实际完成时间。
- 点击处理完成时必须填写开发说明，开发说明支持富文本，可上传开发资料。
- 测试人员点击开始测试：待测试变测试中。
- 测试人员点击测试通过：测试中变测试通过。
- 任务下缺陷必须全部已验证或已关闭，或无缺陷，才允许测试通过。
- 产品经理可手动关闭任务。
- 任务排期支持随时调整，但需要留痕。

### 缺陷

- 缺陷只能从任务表格行内创建，不直接从需求创建。
- 只有测试中、测试通过的任务可以创建缺陷。
- 测试通过的任务创建缺陷时，任务状态退回测试中，关联需求退回开发中。
- 新建缺陷时不选负责人，默认使用开发任务负责人。
- 新建缺陷时不填计划开始和结束时间，排期由开发负责人后续维护。
- 发现环境不在表单里展示，由系统根据关联需求上线状态自动判断：需求已上线为线上，否则为线下。
- 创建线上缺陷时，关联任务的需求上线状态必须是已上线。
- 缺陷等级名称：阻塞、严重、一般、次要。
- 缺陷描述支持富文本。
- 缺陷状态：`TO_FIX -> FIXING -> FIXED -> VERIFIED / CLOSED`。
- 负责人点击开始修复：待修复变修复中。
- 负责人点击已修复：修复中变已修复。
- 测试人员点击验证通过：已修复变已验证。
- 测试人员点击验证未通过：已修复变修复中。
- 只有待修复缺陷可以关闭，关闭按钮只在待修复时展示。
- 已关闭缺陷只有开启功能，开启后变待修复。

### 版本

- 版本记录本次发版上线了哪些需求、修复了哪些缺陷。
- 发布版本只能由产品和测试操作。
- 版本发布要拦截非待上线状态的需求。
- 版本发布要拦截未关闭或未验证的缺陷；当前约定已关闭才能发布。
- 发布成功后，关联需求上线状态改为已上线。

### 项目资料

- 项目资料是项目下子板块，存放业务和技术文档。
- 链接是高频，上传附件是低频。
- 资料表单展示资料描述和上传附件；资料描述使用富文本。
- 资料可关联项目、需求、版本等对象。

## 优先级分数

系统统一叫“优先级分数”，不要再写“分数”“优先级总分”等混用名称。

- 需求优先级分数 = 需求性质基础分 + 变更/优化场景加分 + 时效加分。
- 需求变更/优化若上线状态为已上线，视为更紧急，应有更高加分。
- 当前后端规则：变更待上线 +6，变更已上线 +15；优化待上线 +3，优化已上线 +6。
- 缺陷优先级分数 = 缺陷等级环境基础分 * 关联需求缺陷系数 + 时效加分。
- 不关联需求时，按 P4 的加权值处理。
- 排期相关列表和甘特图默认按优先级分数降序。

## 编号规则

自动编号应使用企业常见业务编码，不用随机数，不精确到时分秒。

- 通用编码格式：`前缀-YYYYMMDD-四位流水`。
- 需求编码格式：`REQ-YYYYMMDD-类型两位字母-两位流水`。
- 需求类型字母示例：功能 `FN`，流程 `PR`，数据 `DA`，报表 `RP`，体验 `UX`，非功能 `NF`。
- 流水通过 `CodeSequence` 管理，按日期和类型递增。

## 表格与排期

- 表格整体能力包括表体、搜索、筛选、排序、分页、工具栏和列操作。
- 当前优先使用 Ant Design Table 作为表格能力底座；表体、列头排序、分页、固定列直接使用其内置能力。
- 搜索组件只承载字符串字段和日期区间字段：字符串用 `Input.Search`，日期区间用 `DatePicker.RangePicker`。
- 筛选组件承载枚举值字段：状态、类型、等级、环境、是否启用等使用 `Select` 多选或 Table filters，不和关键词搜索混用。
- 不要再做自定义搜索栏、筛选浮层或排序面板。
- 操作列固定在右侧。
- 所有主要表格展示创建人、创建时间。
- 排期情况使用可复用甘特图组件。
- 甘特图默认展示未开始和处理中的开发任务/缺陷修复，支持按状态选择展示。
- 甘特图数据按优先级分数降序。
- 甘特图支持直接改起止时间，并支持拖动左右边界调整。
- 只有登录人等于负责人时，才允许保存自己的排期。
- 新建任务表单里的当前临时任务也要进入甘特图，高亮显示；保存排期只同步回表单，提交表单后才创建正式数据。

## UI 约束

- 企业级中后台风格，信息密度合理，重可读性和可维护性。
- 不做花哨渐变、营销式大卡片、装饰性背景。
- 左侧导航和顶部导航要像成熟中后台，不要把退出按钮做得过重。
- 详情页不要复用表单组件；详情页用独立只读展示。
- 表单优先使用弹窗展示，详情可以用侧拉抽屉。
- 表单内选择业务对象的字段必须支持手动输入查询，例如项目、需求、任务、人员、组织、岗位、版本。
- 字典类固定选项不强制搜索。

## 当前容易踩坑

- 不要把需求主状态和上线状态混在同一个筛选项里。
- 不要把岗位当成项目负责人；项目负责人是项目字段。
- 不要把缺陷直接挂需求，缺陷应挂任务，并通过任务关联需求。
- 不要让隐藏过滤、工具栏筛选和表格列头过滤重复生效，否则用户会看到“没筛选但数据少了”。
- 不要把枚举值字段塞进搜索范围；搜索是查文本或日期，筛选才是选状态、类型、等级。
- 不要手写表格搜索、筛选、排序、分页、工具栏相关 UI。
- 不要强行安装和当前 AntD 版本冲突的高级组件。
- 不要因为测试数据问题随意删除用户数据；如果需要清库或重建测试数据，先确认。
